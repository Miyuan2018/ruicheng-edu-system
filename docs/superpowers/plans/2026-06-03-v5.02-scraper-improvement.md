# V5.02 网络抓取试题改进 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 重构网络抓取试题功能 — 三级搜索源回退、多值批量并行搜索、四层数据格式化防护

**Architecture:** DuckDuckGo→百度→LLM 三级搜索回退；前端多选→API 拆分笛卡尔积→asyncio.gather 并行；LLM 返回经强化 Prompt→解析→重试→JSON 修复→Pydantic 校验四层防线；LaTeX 预处理规范数学表达式

**Tech Stack:** Python/httpx/BeautifulSoup/Pydantic, React/Ant Design

**Spec:** `docs/superpowers/specs/2026-06-03-v5.02-scraper-improvement.md`

**Pre-done:** `backend/app/utils/latex.py` — `format_latex()` 函数

---

## Task 1: 重构 scraper.py — 三级搜索源 + LaTeX 预处理

**Files:**
- Modify: `backend/app/services/scraper.py`

- [ ] **Step 1: 添加 DuckDuckGo 搜索函数**

```python
async def _search_duckduckgo(query: str, max_results: int = 10) -> list[str]:
    snippets = []
    try:
        async with httpx.AsyncClient(timeout=httpx.Timeout(15.0)) as client:
            r = await client.get(
                "https://html.duckduckgo.com/html/",
                params={"q": query},
                headers={
                    "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36",
                    "Accept-Language": "zh-CN,zh;q=0.9",
                },
            )
            if r.status_code == 200:
                soup = BeautifulSoup(r.text, "html.parser")
                for item in soup.select(".result__body")[:max_results]:
                    text = item.get_text(" ", strip=True)
                    if len(text) > 20:
                        snippets.append(text)
    except Exception:
        pass
    return snippets
```

- [ ] **Step 2: 添加百度搜索函数**

```python
async def _search_baidu(query: str, max_results: int = 10) -> list[str]:
    snippets = []
    try:
        async with httpx.AsyncClient(timeout=httpx.Timeout(15.0)) as client:
            r = await client.get(
                "https://www.baidu.com/s",
                params={"wd": query, "rn": str(max_results)},
                headers={
                    "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36",
                    "Accept": "text/html,application/xhtml+xml",
                    "Accept-Language": "zh-CN,zh;q=0.9",
                },
            )
            if r.status_code == 200:
                soup = BeautifulSoup(r.text, "html.parser")
                for item in soup.select("div.result, div.result-op")[:max_results]:
                    text = item.get_text(" ", strip=True)
                    if len(text) > 20:
                        snippets.append(text)
    except Exception:
        pass
    return snippets
```

- [ ] **Step 3: 重构 `search_questions` — 三级回退 + LaTeX**

```python
async def search_questions(
    knowledge_point: str, subject: str = "数学", grade_level: str = "G8",
    difficulty: str = "MEDIUM", question_type: str = "SINGLE_CHOICE",
    count: int = 5, provider: str = "deepseek",
) -> list[dict]:
    grade_label = GRADE_LABELS.get(grade_level, grade_level)
    type_label = TYPE_LABELS.get(question_type, "试题")
    query = f"{knowledge_point} {subject} {grade_label} {type_label}"

    # Step 1: 三级搜索回退
    snippets = await _search_duckduckgo(query)
    if not snippets:
        snippets = await _search_baidu(query)
    if not snippets:
        # 兜底：纯 LLM 生成
        snippets = []

    # Step 2: LaTeX 预处理
    from app.utils.latex import format_latex
    snippets = [format_latex(s) for s in snippets]

    # Step 3: LLM 格式化
    context = "\n".join(f"{i+1}. {s}" for i, s in enumerate(snippets[:15]))
    if not snippets:
        context = f"请根据你的知识，生成{count}道关于{knowledge_point}的{subject}{grade_label}{type_label}题目。"
    
    prompt = _build_prompt(knowledge_point, subject, grade_label, type_label, difficulty, question_type, count, context)
    
    try:
        results = await _llm_format(prompt, count, question_type, difficulty, subject, grade_level, provider)
        if not results:
            results = await _llm_format(prompt, count, question_type, difficulty, subject, grade_level, provider)
        return results
    except Exception:
        return []
```

- [ ] **Step 4: 添加 `_build_prompt` — 强化 Prompt**

```python
def _build_prompt(kp: str, subject: str, grade_label: str, type_label: str,
                  diff: str, qtype: str, count: int, context: str) -> str:
    return f"""你是教育试题整理专家。根据以下材料，生成{count}道关于"{kp}"的{subject}{grade_label}{type_label}，难度{diff}。

材料：
{context}

返回格式（严格JSON数组，无其他文字）：
[
  {{
    "title": "题目标题",
    "question_type": "{qtype}",
    "difficulty": "{diff}",
    "score": 5,
    "correct_answer": "...",
    "explanation": "解析说明"
  }}
]

规则：
1. title 必填且非空，长度 2-500
2. question_type 必须是 SINGLE_CHOICE|MULTIPLE_CHOICE|FILL_BLANK|SUBJECTIVE 之一
3. difficulty 必须是 EASY|MEDIUM|HARD 之一
4. score 是正整数
5. correct_answer 必填非空：选择题用"A"/["A","C"]，填空题用答案文本，解答题用关键词
6. 至少{count}道题；材料不足可自行补充
7. 数学表达式用 LaTeX 格式（如 x^{2}）"""
```

- [ ] **Step 5: 改进 `_parse_questions` — JSON 修复 + 字段校验**

```python
def _parse_questions(content: str, qtype: str, diff: str, subj: str, grade: str) -> list[dict]:
    # 1. 优先匹配 ```json 代码块
    match = re.search(r'```(?:json)?\s*(\[.*?\])\s*```', content, re.DOTALL)
    if not match:
        # 2. 正则提取数组
        match = re.search(r'\[.*\]', content, re.DOTALL)
    if not match:
        return []

    json_str = match.group(1) if match.lastindex else match.group()
    
    # 3. JSON 自动修复
    try:
        data = json.loads(json_str)
    except json.JSONDecodeError:
        data = _repair_json(json_str)
        if data is None:
            return []

    if not isinstance(data, list):
        return []

    # 4. 字段校验
    results = []
    for item in data:
        title = str(item.get("title", "")).strip()
        if not title or len(title) < 2:
            continue
        itype = item.get("question_type", qtype)
        if itype not in ("SINGLE_CHOICE", "MULTIPLE_CHOICE", "FILL_BLANK", "SUBJECTIVE"):
            itype = qtype
        idiff = item.get("difficulty", diff)
        if idiff not in ("EASY", "MEDIUM", "HARD"):
            idiff = diff
        score = item.get("score", 5)
        if not isinstance(score, int) or score < 1:
            score = 5
        ca = item.get("correct_answer", "")
        if isinstance(ca, (dict, list)):
            ca = json.dumps(ca, ensure_ascii=False)
        if not ca:
            continue
        results.append({
            "title": title,
            "question_type": itype,
            "difficulty": idiff,
            "subject": item.get("subject", subj),
            "grade_level": item.get("grade_level", grade),
            "score": score,
            "correct_answer": str(ca),
            "explanation": item.get("explanation", ""),
            "source_url": "web_scrape",
        })
    return results


def _repair_json(json_str: str) -> list | None:
    """Attempt to repair truncated/incomplete JSON."""
    # 补全末尾缺失的 ] }
    stripped = json_str.strip()
    # 计数未闭合的括号
    open_braces = stripped.count('{') - stripped.count('}')
    open_brackets = stripped.count('[') - stripped.count(']')
    # 去掉末尾可能截断的半截字符串
    if stripped.endswith('"') and not stripped.endswith('}"'):
        pass  # 可能正常
    repaired = stripped + '}' * open_braces + ']' * open_brackets
    try:
        return json.loads(repaired)
    except Exception:
        return None
```

- [ ] **Step 6: 删除死代码**

删除以下不再使用的函数：`_search_bing_text`、`_search_bing`、`_extract_question_from_text`、`_build_question`

- [ ] **Step 7: 验证**

```bash
cd backend && /home/nvidia/conda_workspace/bin/python -c "from app.services.scraper import search_questions, _search_duckduckgo, _search_baidu; print('OK')"
```

- [ ] **Step 8: Commit**

```bash
git add backend/app/services/scraper.py
git commit -m "refactor: V5.02 scraper 三级搜索回退+强化Prompt+JSON修复+LaTeX预处理

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Task 2: ScrapedQuestion Pydantic 校验模型

**Files:**
- Modify: `backend/app/schemas/question.py`

- [ ] **Step 1: 添加 ScrapedQuestion 模型**

在文件末尾添加：

```python
class ScrapedQuestion(BaseModel):
    """网络抓取题目校验模型 — 逐字段验证 LLM 返回"""
    title: str = Field(..., min_length=2, max_length=500)
    question_type: str = Field(..., pattern="^(SINGLE_CHOICE|MULTIPLE_CHOICE|FILL_BLANK|SUBJECTIVE)$")
    difficulty: str = Field(..., pattern="^(EASY|MEDIUM|HARD)$")
    score: int = Field(default=5, ge=1)
    correct_answer: str = Field(..., min_length=1)
    explanation: str = Field(default="")
    subject: str = Field(default="")
    grade_level: str = Field(default="")
```

- [ ] **Step 2: 验证**

```bash
cd backend && /home/nvidia/conda_workspace/bin/python -c "
from app.schemas.question import ScrapedQuestion
# 正常
q = ScrapedQuestion(title='test', question_type='SINGLE_CHOICE', difficulty='EASY', correct_answer='A')
print('Valid:', q.title)
# 异常
try:
    ScrapedQuestion(title='', question_type='SINGLE_CHOICE', difficulty='EASY', correct_answer='A')
except Exception as e:
    print('Invalid caught:', type(e).__name__)
"```

- [ ] **Step 3: Commit**

```bash
git add backend/app/schemas/question.py
git commit -m "feat: V5.02 添加 ScrapedQuestion Pydantic 校验模型

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Task 3: `/scrape` 端点 — 多值参数 + 批量并行

**Files:**
- Modify: `backend/app/api/v1/endpoints/question_admin.py:391-484`

- [ ] **Step 1: 修改函数签名**

```python
@router.post("/scrape")
async def start_scrape(
    knowledge_points: str,     # 逗号分隔，如 "勾股定理,一次函数"
    count: int = 5,            # 每个子任务的数量
    subject: str = "数学",
    grade_levels: str = "G8",  # 逗号分隔，如 "G7,G8,G9"
    difficulty: str = "MEDIUM",
    question_types: str = "SINGLE_CHOICE",  # 逗号分隔
    current_user = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
```

- [ ] **Step 2: 拆分笛卡尔积 + 并行搜索**

```python
    kps = [k.strip() for k in knowledge_points.split(",") if k.strip()]
    gls = [g.strip() for g in grade_levels.split(",") if g.strip()]
    qts = [q.strip() for q in question_types.split(",") if q.strip()]

    tasks = []
    for kp in kps:
        for gl in gls:
            for qt in qts:
                tasks.append((kp, gl, qt))

    # 并行执行
    import asyncio
    all_results = []
    async def _search_one(kp, gl, qt):
        return await search_questions(
            knowledge_point=kp, subject=subject, grade_level=gl,
            difficulty=difficulty, question_type=qt, count=count,
        )

    batches = await asyncio.gather(*[_search_one(kp, gl, qt) for kp, gl, qt in tasks], return_exceptions=True)
    for b in batches:
        if isinstance(b, list):
            all_results.extend(b)
```

- [ ] **Step 3: 保持异步 Celery 路径兼容**

批量搜索模式下，如果 tasks 数量 > 1，走同步路径（不拆分 Celery 任务）。单任务时保持原有 Celery 异步逻辑。

- [ ] **Step 4: 合并批量存储**

```python
    saved = 0
    grade_json_base = {"scope": "grade", "grades": gls, "knowledge_points": kps}
    if kps:
        grade_json_base["chapter"] = kps[0]

    for q_data in all_results:
        db.add(Question(
            title=q_data.get("title", ""),
            question_type=q_data.get("question_type", question_types.split(",")[0]),
            difficulty=q_data.get("difficulty", difficulty),
            subject=q_data.get("subject", subject),
            grade_level=grade_json_base,
            score=q_data.get("score", 5),
            correct_answer=q_data.get("correct_answer", ""),
            explanation=q_data.get("explanation", ""),
            source="SCRAPED", review_status="PENDING",
            created_by=current_user.id,
        ))
        saved += 1

    await db.commit()
    return {"ok": True, "count": saved, "tasks": len(tasks),
            "search_params": {"知识点": kps, "学科": subject,
                              "年级": gls, "难度": difficulty,
                              "题型": qts, "每任务数量": count},
            "error": None if saved > 0 else "未找到试题"}
```

- [ ] **Step 5: 验证**

```bash
cd backend && /home/nvidia/conda_workspace/bin/python -c "from app.main import app; print('OK')"
```

- [ ] **Step 6: Commit**

```bash
git add backend/app/api/v1/endpoints/question_admin.py
git commit -m "feat: V5.02 /scrape 端点多值参数+批量并行搜索

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Task 4: 前端表单改为多选

**Files:**
- Modify: `frontend/src/pages/admin/QuestionAdminPage.tsx`

- [ ] **Step 1: 修改 form 字段为多选**

找到抓取表单（scrape Tab 内），将单字段改为多选：

```tsx
// 改前 - 知识点 Input
<Form.Item name="knowledge_point" label="知识点" rules={[{ required: true }]}>
  <Input placeholder="多知识点综合用逗号分割" />
</Form.Item>

// 改后 - 知识点 Select mode="tags"
<Form.Item name="knowledge_points" label="知识点" rules={[{ required: true }]}>
  <Select mode="tags" placeholder="输入知识点后回车，可添加多个"
    tokenSeparators={[',', '，']} style={{ width: '100%' }} />
</Form.Item>

// 改前 - 年级 Select 单选
<Form.Item name="grade_level" label="年级" initialValue="G8">
  <Select options={gradeOptions} />
</Form.Item>

// 改后 - 年级 Select mode="multiple"
<Form.Item name="grade_levels" label="年级" initialValue={['G8']}>
  <Select mode="multiple" options={gradeOptions} allowClear />
</Form.Item>

// 改前 - 题型 Select 单选
<Form.Item name="question_type" label="题型" initialValue="SINGLE_CHOICE">
  <Select options={typeOptions} />
</Form.Item>

// 改后 - 题型 Select mode="multiple"
<Form.Item name="question_types" label="题型" initialValue={['SINGLE_CHOICE']}>
  <Select mode="multiple" options={typeOptions} allowClear />
</Form.Item>
```

- [ ] **Step 2: 更新 `handleScrape` 参数名**

将请求参数从旧单值名改为新逗号分隔名，数组 join 成逗号分隔字符串：

```typescript
const values = scrapeForm.getFieldsValue();
const params = {
  knowledge_points: Array.isArray(values.knowledge_points)
    ? values.knowledge_points.join(',') : values.knowledge_points,
  grade_levels: (values.grade_levels || ['G8']).join(','),
  question_types: (values.question_types || ['SINGLE_CHOICE']).join(','),
  count: values.count || 5,
  subject: values.subject || '数学',
  difficulty: values.difficulty || 'MEDIUM',
};
```

- [ ] **Step 3: 更新搜索条件预览**

```typescript
const updateScrapeConditions = () => {
    const v = scrapeForm.getFieldsValue();
    const kps = Array.isArray(v.knowledge_points) ? v.knowledge_points : [v.knowledge_points].filter(Boolean);
    const gls = Array.isArray(v.grade_levels) ? v.grade_levels : [v.grade_levels].filter(Boolean);
    const qts = Array.isArray(v.question_types) ? v.question_types : [v.question_types].filter(Boolean);
    const taskCount = kps.length * gls.length * qts.length;
    setScrapeConditions(
      `将拆分 ${taskCount} 个搜索任务:\n` +
      kps.flatMap(kp => gls.flatMap(gl => qts.map(qt =>
        `  知识点: ${kp} → 年级: ${gl} → 题型: ${TYPE_LABELS[qt] || qt} ×${v.count || 5}`
      ))).join('\n')
    );
};
```

- [ ] **Step 4: 验证 TypeScript**

```bash
cd frontend && npx tsc --noEmit --pretty false 2>&1 | head -5
```

- [ ] **Step 5: Commit**

```bash
git add frontend/src/pages/admin/QuestionAdminPage.tsx
git commit -m "feat: V5.02 抓取表单改为多选+批量并行预览

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## 执行顺序

```
Task 2 (ScrapedQuestion model) → Task 1 (scraper.py 重构)
  → Task 3 (/scrape 端点) → Task 4 (前端表单)
```

Task 2 先做（新模型在 Task 1 中可选引用），Task 1 是核心重构，Task 3/4 是上下游对接。
