# V4.1 组卷流程修正 — 实施计划

> **For agentic workers:** 使用 superpowers:subagent-driven-development 逐个任务执行。

**Goal:** 结构变更联动（缺口补充+多余标记）、一键选题、换题约束链、预览格式统一

**Architecture:** 后端 swap 端点增加学科/年级/知识点约束链；前端 RecommendStep 增加结构变更检测、一键选题按钮、手工选题入口；预览组件统一选项换行格式

**Tech Stack:** React 19 + TypeScript + FastAPI + SQLAlchemy

---

### Task 1: 后端 swap 端点约束链

**File:** `backend/app/api/v1/endpoints/exam_papers.py:1218-1293`

在 swap 端点中，将候选查询改为逐级放宽约束链：

```python
@router.post("/{paper_id}/questions/{question_id}/swap")
async def swap_question(paper_id, question_id, current_user=Depends(get_current_user), db=Depends(get_db)):
    _check_teacher_or_admin(current_user)

    result = await db.execute(select(Question).where(Question.id == question_id))
    current = result.scalar_one_or_none()
    if not current:
        raise HTTPException(404, detail="题目不存在")

    paper_check = await db.execute(select(ExamPaper).where(ExamPaper.id == paper_id))
    paper = paper_check.scalar_one_or_none()
    if not paper:
        raise HTTPException(404, detail="试卷不存在")

    # 获取已用题目ID
    used_result = await db.execute(
        select(ExamPaperUnitQuestion.question_id).join(ExamPaperUnit).where(ExamPaperUnit.exam_paper_id == paper_id)
    )
    used = {str(r[0]) for r in used_result.fetchall()}

    # 逐级放宽约束链
    subject = paper.subject
    grade_level = paper.grade_level
    grades = grade_level.get("grades", []) if isinstance(grade_level, dict) else []
    kn_ids = paper.knowledge_node_ids or []

    candidates = []
    # Level 1: 学科 + 年级 + 知识点
    if candidates == [] and subject and grades and kn_ids:
        candidates = await _query_swap_candidates(db, current.question_type, current.difficulty, used, subject=subject, grades=grades, kn_ids=kn_ids)
    # Level 2: 学科 + 年级
    if candidates == [] and subject and grades:
        candidates = await _query_swap_candidates(db, current.question_type, current.difficulty, used, subject=subject, grades=grades)
    # Level 3: 学科 + 题型 + 难度(无年级限制)
    if candidates == []:
        candidates = await _query_swap_candidates(db, current.question_type, current.difficulty, used, subject=subject)

    if not candidates:
        return {"alternatives": []}

    top = min(3, len(candidates))
    alternatives = [{"question_id": str(q.id), "title": (q.title or "")[:120], "difficulty": q.difficulty or "", "question_type": q.question_type or "", "score": q.score or 0} for q in candidates[:top]]
    return {"alternatives": alternatives}
```

新增辅助函数（放在 swap 端点之前）：

```python
async def _query_swap_candidates(db, question_type, difficulty, used_ids, subject=None, grades=None, kn_ids=None):
    """查询换题候选，支持可选约束"""
    conditions = [Question.is_active == True, Question.review_status == "APPROVED", Question.question_type == question_type]
    if difficulty:
        conditions.append(Question.difficulty == difficulty)
    if subject:
        conditions.append(Question.subject == subject)
    if grades:
        from sqlalchemy import cast, String
        grade_conditions = []
        for g in grades:
            grade_conditions.append(Question.grade_level["grades"].astext.contains(g))
        if grade_conditions:
            from sqlalchemy import or_
            conditions.append(or_(*grade_conditions))
    if kn_ids:
        from app.models.knowledge_node import QuestionKnowledgeNode
        kn_qids = await db.execute(select(QuestionKnowledgeNode.question_id).where(QuestionKnowledgeNode.knowledge_node_id.in_(kn_ids)))
        kn_qid_set = {str(r[0]) for r in kn_qids.fetchall()}
        if kn_qid_set:
            conditions.append(Question.id.in_(kn_qid_set))

    result = await db.execute(select(Question).where(*conditions))
    return [q for q in result.scalars().all() if str(q.id) not in used_ids]
```

- [ ] **Step 1: 修改 swap 端点 + 添加辅助函数**
- [ ] **Step 2: 重启后端验证**
- [ ] **Step 3: 提交** `feat: swap 端点逐级放宽约束链 — 学科/年级/知识点`

---

### Task 2: RecommendStep — 结构变更联动 + 一键选题 + 换题约束

**File:** `frontend/src/pages/papers/steps/RecommendStep.tsx`

#### 2a: 结构变更检测

在 `useEffect` 自动触发生成时，检测缺口和多余：

```tsx
// 替换现有的 useEffect
useEffect(() => {
  if (!paper?.id || hasAutoTriggered.current) return;
  const units = paper?.units || [];
  const hasConfigs = units.some(u => (u.question_config || []).some(c => (c.count || 0) > 0));
  if (!hasConfigs) return;
  hasAutoTriggered.current = true;

  // 检测是否有缺口需要补充
  let needsFill = false;
  units.forEach(u => {
    (u.question_config || []).forEach(cfg => {
      const existing = (u.questions || []).filter(q => q.question_type === cfg.question_type).length;
      if ((cfg.count || 0) > existing) needsFill = true;
    });
  });
  if (needsFill) {
    handleGenerate(true);  // force=true: 补充缺口
  }
}, [paper?.id]);
```

修改 `handleGenerate` 支持缺口模式：已有题目保留，只补充不足部分。新增 `handleFullRegenerate` 一键选题。

#### 2b: 一键选题按钮

```tsx
// 替换当前按钮区
<Button type="primary" icon={<SyncOutlined />} loading={loading} onClick={() => handleFullRegenerate()}>
  一键选题
</Button>
```

新增函数：

```tsx
const handleFullRegenerate = async () => {
  if (!paper?.id) { message.warning('请先保存基本信息'); return; }
  setLoading(true);
  try {
    clearAllQuestions();
    setGenerateReport(null);
    await handleGenerate(true);
  } finally {
    setLoading(false);
  }
};
```

#### 2c: 多余标记

在渲染题型分区时检测多余：

```tsx
// 在题型分区渲染中
const configCount = unit?.question_config?.find(c => c.question_type === qtype)?.count || 0;
const surplus = questions.length - configCount;

{surplus > 0 && (
  <div style={{ color: '#ff4d4f', fontSize: 12, marginTop: 4 }}>
    已选 {questions.length}/{configCount} 题，超出 {surplus} 题（请手动删除多余试题）
  </div>
)}
```

多余试题红色标记：

```tsx
const isExtra = configCount > 0 && qi >= configCount;
// ...
<div style={{ borderLeft: isExtra ? '3px solid #ff4d4f' : 'none', paddingLeft: isExtra ? 8 : 0 }}>
  {isExtra && <Tag color="error" style={{ fontSize: 10 }}>多余</Tag>}
</div>
```

#### 2d: 换题后手工选题按钮

在换题成功后的题目行末尾：

```tsx
{!isExtra && (
  <Button size="small" type="link" onClick={() => setManualSelectQid(q.question_id)}>
    手工选题
  </Button>
)}
```

（手工选题弹窗留到后续实现，先放按钮入口）

#### 2e: 换题传参

`handleSwap` 中调用 API 不变（后端已支持约束链）。

- [ ] **Step 1: 修改 RecommendStep.tsx**
- [ ] **Step 2: `npx tsc --noEmit`**
- [ ] **Step 3: 提交** `feat: RecommendStep 结构变更联动+一键选题+多余标记`

---

### Task 3: 预览格式 — 选项每项单独一行

**Files:**
- `frontend/src/pages/papers/steps/PreviewStep.tsx` — renderQuestion 函数
- `frontend/src/pages/papers/PaperTemplatePreview.tsx` — renderQuestion 函数
- `frontend/src/pages/papers/PrintPreviewPage.tsx` — 题目渲染

当前选项渲染使用 `flex` 布局或单行显示。改为每项单独一行：

```tsx
{isChoice && options.length > 0 && (
  <div style={{ marginTop: 4, marginLeft: 24, fontSize: 13 }}>
    {options.map((opt: any, idx: number) => {
      const label = opt.label || opt.id || String.fromCharCode(65 + idx);
      const text = opt.text || opt.content || '';
      return <div key={label} style={{ marginBottom: 2 }}>{label + '. ' + text}</div>;
    })}
  </div>
)}
```

三个文件的 renderQuestion/题目渲染部分统一为此格式。

- [ ] **Step 1: 修改三个文件的选项渲染**
- [ ] **Step 2: `npx tsc --noEmit`**
- [ ] **Step 3: 提交** `feat: 预览/打印 选择题选项每项单独一行`

---

### Task 4: 端到端验证

- [ ] **Step 1: 启动服务**

```bash
curl http://localhost:8001/health  # 确认 OK
```

- [ ] **Step 2: 验证流程**

1. 新建数学试卷 → 设置结构(单选5题/填空3题)
2. Step3 自动选题 → 确认选中
3. 退回 Step2 → 减少单选为3题 → 增加填空为5题
4. Step3 → 验证单选多余标记、填空自动补齐
5. 点击"一键选题" → 验证全部清空重新生成
6. 某题点[↻] → 验证换题候选限定学科/年级
7. Step4 预览 → 验证选项每项单独一行
8. 导出 Word → 验证格式一致

- [ ] **Step 3: 提交** `chore: V4.1 端到端验证通过`
