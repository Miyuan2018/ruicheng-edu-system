# V4.3 草稿表 + 保存/发布流程重构 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 草稿存入独立 `exam_paper_drafts` 表（JSONB），与正式试卷主表隔离；保存发布流程重构，状态模型新增 READY。

**Architecture:** 新增 drafts 表 + CRUD API；前端 autoSave 改走草稿表，saveAll 写主表后清理草稿；loadDraft 加草稿检测弹窗；PreviewFinalizeStep 按钮改 [保存] + [保存并发布]。

**Tech Stack:** FastAPI + PostgreSQL + React 19 + TypeScript + Zustand 5

---

### Task 1: 后端 — 新建 exam_paper_drafts 模型 + 迁移 + CRUD API

**Files:**
- Create: `backend/app/models/exam_paper_draft.py`
- Create: `backend/alembic/versions/006_v43_drafts.py`
- Create: `backend/app/schemas/exam_paper_draft.py`
- Create: `backend/app/api/v1/endpoints/drafts.py`
- Modify: `backend/app/api/v1/api.py`

- [ ] **Step 1: 创建模型**

```python
# backend/app/models/exam_paper_draft.py
import uuid
from sqlalchemy import Column, String, DateTime, ForeignKey, UniqueConstraint
from sqlalchemy.dialects.postgresql import UUID, JSONB
from sqlalchemy.sql import func
from app.db.base import Base

class ExamPaperDraft(Base):
    __tablename__ = "exam_paper_drafts"

    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    user_id = Column(UUID(as_uuid=True), ForeignKey("admins.id"), nullable=False)
    paper_id = Column(UUID(as_uuid=True), ForeignKey("exam_papers.id"), nullable=True)
    data = Column(JSONB, nullable=False)
    created_at = Column(DateTime, server_default=func.now())
    updated_at = Column(DateTime, server_default=func.now(), onupdate=func.now())

    __table_args__ = (
        UniqueConstraint("user_id", "paper_id", name="uq_user_paper_draft"),
    )
```

- [ ] **Step 2: 创建迁移**

```python
# backend/alembic/versions/006_v43_drafts.py
"""V4.3: exam_paper_drafts table + DRAFT → READY status

Revision ID: 006_v43_drafts
"""
from alembic import op
import sqlalchemy as sa
from sqlalchemy.dialects.postgresql import UUID, JSONB

revision = '006_v43_drafts'
down_revision = '005_v4_show_units_timer'
branch_labels = None
depends_on = None

def upgrade():
    # 1. Create drafts table
    op.create_table(
        'exam_paper_drafts',
        sa.Column('id', UUID(as_uuid=True), primary_key=True, server_default=sa.text('gen_random_uuid()')),
        sa.Column('user_id', UUID(as_uuid=True), sa.ForeignKey('admins.id'), nullable=False),
        sa.Column('paper_id', UUID(as_uuid=True), sa.ForeignKey('exam_papers.id'), nullable=True),
        sa.Column('data', JSONB, nullable=False),
        sa.Column('created_at', sa.DateTime, server_default=sa.func.now()),
        sa.Column('updated_at', sa.DateTime, server_default=sa.func.now()),
        sa.UniqueConstraint('user_id', 'paper_id', name='uq_user_paper_draft'),
    )

    # 2. Migrate existing DRAFT papers to drafts table
    conn = op.get_bind()
    drafts = conn.execute(sa.text("SELECT id, created_by, title, subject, grade_level, total_score, duration_minutes, status, subtitle, instructions, description, show_units, per_unit_timer, difficulty_ratio, knowledge_node_ids FROM exam_papers WHERE status = 'DRAFT'")).fetchall()
    for row in drafts:
        conn.execute(
            sa.text("INSERT INTO exam_paper_drafts (user_id, paper_id, data) VALUES (:uid, :pid, :data) ON CONFLICT DO NOTHING"),
            {"uid": row.created_by, "pid": row.id, "data": '{}'}
        )

    # 3. Update CheckConstraint: DRAFT → READY
    op.execute("ALTER TABLE exam_papers DROP CONSTRAINT IF EXISTS check_exam_papers_status")
    op.execute("UPDATE exam_papers SET status = 'READY' WHERE status = 'DRAFT'")
    op.execute("ALTER TABLE exam_papers ADD CONSTRAINT check_exam_papers_status CHECK (status IN ('READY', 'PUBLISHED', 'ARCHIVED'))")

def downgrade():
    op.execute("ALTER TABLE exam_papers DROP CONSTRAINT IF EXISTS check_exam_papers_status")
    op.execute("ALTER TABLE exam_papers ADD CONSTRAINT check_exam_papers_status CHECK (status IN ('DRAFT', 'PUBLISHED', 'ARCHIVED'))")
    op.drop_table('exam_paper_drafts')
```

- [ ] **Step 3: 创建 Pydantic schema**

```python
# backend/app/schemas/exam_paper_draft.py
from pydantic import BaseModel, Field
from typing import Optional
from datetime import datetime

class DraftCreate(BaseModel):
    paper_id: Optional[str] = None
    data: dict

class DraftResponse(BaseModel):
    id: str
    user_id: str
    paper_id: Optional[str] = None
    data: dict
    created_at: Optional[datetime] = None
    updated_at: Optional[datetime] = None

    class Config:
        from_attributes = True
```

- [ ] **Step 4: 创建 drafts API**

```python
# backend/app/api/v1/endpoints/drafts.py
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select, delete
from app.db.session import get_db
from app.core.security import get_current_user
from app.models.exam_paper_draft import ExamPaperDraft
from app.schemas.exam_paper_draft import DraftCreate, DraftResponse

router = APIRouter()

def _check_teacher_or_admin(user):
    if user.user_type not in ("TEACHER", "QUESTION_ADMIN", "SYS_ADMIN"):
        raise HTTPException(403, detail="权限不足")

@router.post("", response_model=DraftResponse)
async def save_draft(
    body: DraftCreate,
    current_user=Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
    """创建或覆盖草稿"""
    _check_teacher_or_admin(current_user)
    # 查找已有草稿
    existing = await db.execute(
        select(ExamPaperDraft).where(
            ExamPaperDraft.user_id == current_user.id,
            ExamPaperDraft.paper_id == body.paper_id,
        )
    )
    draft = existing.scalar_one_or_none()
    if draft:
        draft.data = body.data
    else:
        draft = ExamPaperDraft(
            user_id=current_user.id,
            paper_id=body.paper_id,
            data=body.data,
        )
        db.add(draft)
    await db.commit()
    await db.refresh(draft)
    return draft

@router.get("", response_model=list[DraftResponse])
async def list_drafts(
    paper_id: str | None = None,
    current_user=Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
    """获取草稿列表"""
    _check_teacher_or_admin(current_user)
    q = select(ExamPaperDraft).where(ExamPaperDraft.user_id == current_user.id)
    if paper_id:
        q = q.where(ExamPaperDraft.paper_id == paper_id)
    result = await db.execute(q.order_by(ExamPaperDraft.updated_at.desc()))
    return result.scalars().all()

@router.delete("/{draft_id}", status_code=204)
async def delete_draft(
    draft_id: str,
    current_user=Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
    """删除草稿"""
    _check_teacher_or_admin(current_user)
    await db.execute(delete(ExamPaperDraft).where(ExamPaperDraft.id == draft_id))
    await db.commit()
    return None
```

- [ ] **Step 5: 注册路由**

```python
# 在 backend/app/api/v1/api.py 添加
from app.api.v1.endpoints import drafts
api_router.include_router(drafts.router, prefix="/drafts", tags=["drafts"])
```

- [ ] **Step 6: 运行迁移并验证**

```bash
cd /home/nvidia/Desktop/ruicheng-edu-system/backend
alembic upgrade head
```

- [ ] **Step 7: 提交**

```bash
git -C /home/nvidia/Desktop/ruicheng-edu-system add backend/app/models/exam_paper_draft.py backend/alembic/versions/006_v43_drafts.py backend/app/schemas/exam_paper_draft.py backend/app/api/v1/endpoints/drafts.py backend/app/api/v1/api.py
git -C /home/nvidia/Desktop/ruicheng-edu-system commit -m "feat: 新增 exam_paper_drafts 表 + CRUD API，DRAFT → READY 状态迁移"
```

---

### Task 2: 后端 — save-all 默认 status=READY，清理 DRAFT 引用

**Files:**
- Modify: `backend/app/schemas/exam_paper.py`
- Modify: `backend/app/models/exam_paper.py`

- [ ] **Step 1: 更新 ExamPaper 模型 status 默认值**

```python
# backend/app/models/exam_paper.py 第 55 行
status = Column(String(20), nullable=False, default='READY')  # 原来是 'DRAFT'
```

- [ ] **Step 2: 更新 ExamPaperFullSave schema 的 status 默认值**

```python
# backend/app/schemas/exam_paper.py 第 102 行
status: str = "READY"  # 原来是 "DRAFT"
```

- [ ] **Step 3: 验证语法**

```bash
~/conda_workspace/bin/python -c "import py_compile; py_compile.compile('app/schemas/exam_paper.py', doraise=True); py_compile.compile('app/models/exam_paper.py', doraise=True); print('OK')"
```

- [ ] **Step 4: 提交**

```bash
git -C /home/nvidia/Desktop/ruicheng-edu-system add backend/app/schemas/exam_paper.py backend/app/models/exam_paper.py
git -C /home/nvidia/Desktop/ruicheng-edu-system commit -m "refactor: status 默认值 DRAFT → READY"
```

---

### Task 3: 前端 — 新增 draftApi

**Files:**
- Create: `frontend/src/api/drafts.ts`

- [ ] **Step 1: 创建 draftApi**

```typescript
// frontend/src/api/drafts.ts
import apiClient from './client';

export const draftApi = {
  save: (paperId: string | null, data: any) =>
    apiClient.post('/drafts', { paper_id: paperId, data }),

  getByPaper: (paperId: string) =>
    apiClient.get(`/drafts?paper_id=${paperId}`),

  list: () =>
    apiClient.get('/drafts'),

  delete: (draftId: string) =>
    apiClient.delete(`/drafts/${draftId}`),
};
```

- [ ] **Step 2: TypeScript 编译检查**

```bash
cd /home/nvidia/Desktop/ruicheng-edu-system/frontend
npm run build 2>&1 | grep "drafts" | head -5
```

- [ ] **Step 3: 提交**

```bash
git -C /home/nvidia/Desktop/ruicheng-edu-system add frontend/src/api/drafts.ts
git -C /home/nvidia/Desktop/ruicheng-edu-system commit -m "feat: 新增 draftApi（save/get/list/delete）"
```

---

### Task 4: 前端 Store — autoSave 改走草稿表，saveAll 写主表+清草稿，loadDraft 加草稿检测

**Files:**
- Modify: `frontend/src/store/paperEditor.ts`

- [ ] **Step 1: 新增 import**

在文件顶部 import 区添加：
```typescript
import { draftApi } from '../api/drafts';
```

- [ ] **Step 2: 重写 autoSave**

替换 `autoSave` 函数（原约第 530 行）：
```typescript
  autoSave: async () => {
    const { paper, dirty } = get();
    if (!paper || !dirty) return;
    set({ saving: true });
    try {
      await draftApi.save(paper.id || null, paper);
      set({ saving: false, dirty: false, lastSaved: new Date() });
    } catch {
      set({ saving: false });
    }
  },
```

- [ ] **Step 3: 重写 saveAll**

替换 `saveAll` 函数（原约第 554 行）：
```typescript
  saveAll: async () => {
    const { paper } = get();
    if (!paper || !paper.id) return;
    set({ saving: true });
    try {
      const cleanPaper = {
        ...paper,
        units: paper.units.map(u => ({
          name: u.name,
          description: u.description,
          position: u.position,
          time_limit_minutes: u.time_limit_minutes,
          question_config: u.question_config,
          questions: (u.questions || []).map(q => ({
            question_id: q.question_id,
            question_type: q.question_type,
            position: q.position,
            score: q.score,
          })),
        })),
      };
      await paperApi.saveAll(paper.id, cleanPaper);
      // 保存成功后清理对应草稿
      try {
        const drafts = await draftApi.getByPaper(paper.id);
        if (Array.isArray(drafts)) {
          for (const d of drafts) {
            await draftApi.delete(d.id).catch(() => {});
          }
        }
      } catch {}
      set({ saving: false, dirty: false, lastSaved: new Date() });
    } catch (e) {
      set({ saving: false });
      throw e;
    }
  },
```

- [ ] **Step 4: 在 PaperEditorState interface 添加 pendingDraft 字段**

在 interface 顶部 state 区添加：
```typescript
  pendingDraft: any | null;
```

在 `newEmptyPaper` 调用和 `initNew` 中加 `pendingDraft: null`。

- [ ] **Step 5: 重写 loadDraft**

替换 `loadDraft` 函数（原约第 93 行）：
```typescript
  loadDraft: async (id: string) => {
    set({ loading: true });
    try {
      // 先检查是否有未完成草稿
      let draftData = null;
      try {
        const drafts = await draftApi.getByPaper(id);
        const draft = Array.isArray(drafts) ? drafts[0] : null;
        draftData = draft?.data || null;
      } catch {}

      const resp = await paperApi.preview(id);
      const previewData = resp.data || resp;
      const paperData = previewData.paper || previewData;

      // 规范化题目数据
      const units = (previewData.units || paperData.units || []).map((unit: any) => ({
        ...unit,
        questions: (unit.questions || []).map((q: any) => ({
          id: q.id || '',
          question_id: q.question_id || q.id || '',
          question_type: q.question_type || '',
          position: q.position || 0,
          score: q.score || 0,
          question: {
            id: q.question_id || q.id || '',
            title: q.title || '',
            question_type: q.question_type || '',
            difficulty: q.difficulty || '',
            subject: q.subject || paperData.subject || '',
            options: normalizeOpts(q.options),
            correct_answer: q.correct_answer || '',
            explanation: q.explanation || '',
          },
          recommendation_tags: q.recommendation_tags || [],
          alternatives: q.alternatives || [],
        })),
      }));
      // 同步 config 分数
      units.forEach((unit: any) => {
        (unit.question_config || []).forEach((cfg: any) => {
          if (cfg.score_per_question != null) {
            unit.questions = (unit.questions || []).map((q: any) =>
              q.question_type === cfg.question_type ? { ...q, score: cfg.score_per_question } : q
            );
          }
        });
      });

      set({
        paper: { ...paperData, units },
        pendingDraft: draftData || null,
        currentStep: 0,
        loading: false,
        dirty: false,
      });
    } catch {
      set({ loading: false });
    }
  },
```

- [ ] **Step 6: TypeScript 编译检查**

```bash
cd /home/nvidia/Desktop/ruicheng-edu-system/frontend
npm run build 2>&1 | grep "paperEditor" | head -5
```

- [ ] **Step 7: 提交**

```bash
git -C /home/nvidia/Desktop/ruicheng-edu-system add frontend/src/store/paperEditor.ts
git -C /home/nvidia/Desktop/ruicheng-edu-system commit -m "feat: autoSave→草稿表, saveAll→主表+清草稿, loadDraft→草稿检测"
```

---

### Task 5: PreviewFinalizeStep — 按钮改 [保存] + [保存并发布]

**Files:**
- Modify: `frontend/src/pages/papers/steps/PreviewFinalizeStep.tsx`

- [ ] **Step 1: 替换按钮区**

替换原来的 publishMode Radio + 确认入库按钮（原第 132-242 行区域）为：

在工具栏位置（`<div style={{ display: 'flex', justifyContent: 'space-between'...`）：
```typescript
{/* Toolbar */}
<div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 16 }}>
  <Space>
    <Button type="primary" size="large" icon={<CheckCircleOutlined />} loading={submitting}
      onClick={async () => {
        if (!paper?.id) { message.warning('请先完成前序步骤'); return; }
        setSubmitting(true);
        try {
          await saveAll();
          message.success('已保存');
          setDirty(false);
          setTimeout(() => navigate('/papers'), 500);
        } catch (err: any) {
          const detail = err?.response?.data?.detail;
          message.error(typeof detail === 'string' ? detail : '保存失败');
        } finally { setSubmitting(false); }
      }}
    >保存</Button>
    <Button type="primary" size="large" ghost icon={<CheckCircleOutlined />} loading={submitting}
      onClick={async () => {
        if (!paper?.id) { message.warning('请先完成前序步骤'); return; }
        setSubmitting(true);
        try {
          await saveAll();
          await apiClient.post(`/exam-papers/${paper.id}/publish`, {
            class_ids: selectedClasses,
            note: publishNote || undefined,
          });
          message.success('已保存并发布');
          setDirty(false);
          setTimeout(() => navigate('/papers'), 500);
        } catch (err: any) {
          const detail = err?.response?.data?.detail;
          message.error(typeof detail === 'string' ? detail : '保存失败');
        } finally { setSubmitting(false); }
      }}
    >保存并发布</Button>
  </Space>
  <Space>
    <Tooltip title="导出 Word"><Button icon={<FileWordOutlined />} onClick={() => handleExport('word')}>导出 Word</Button></Tooltip>
    <Tooltip title="导出 PDF"><Button icon={<FilePdfOutlined />} onClick={() => handleExport('pdf')}>导出 PDF</Button></Tooltip>
    <Tooltip title="打印"><Button icon={<PrinterOutlined />} onClick={handlePrint}>打印</Button></Tooltip>
  </Space>
</div>
```

- [ ] **Step 2: 移除不再需要的状态**

删除：`publishMode`, `publishNote`, `classLoading`, `classOptions`, `loadClasses`, `handlePublishToggle`。

保留：`selectedClasses`, `publishNote`（用于"保存并发布"时传 class_ids + note）。

- [ ] **Step 3: 移除 Cards 不再需要的 Tab1 区域**

Tab1 "入库设置" 只保留：试卷概要 Card + 难度偏离 Card + 选题清单 Card。移除"发布设置" Card。

- [ ] **Step 4: TypeScript 编译检查**

```bash
cd /home/nvidia/Desktop/ruicheng-edu-system/frontend
npm run build 2>&1 | grep "PreviewFinalize" | head -5
```

- [ ] **Step 5: 提交**

```bash
git -C /home/nvidia/Desktop/ruicheng-edu-system add frontend/src/pages/papers/steps/PreviewFinalizeStep.tsx
git -C /home/nvidia/Desktop/ruicheng-edu-system commit -m "feat: 按钮改[保存]+[保存并发布]，移除发布设置面板"
```

---

### Task 6: PaperWizardPage — 草稿恢复弹窗

**Files:**
- Modify: `frontend/src/pages/papers/PaperWizardPage.tsx`

- [ ] **Step 1: 解构 pendingDraft**

在 store 解构中添加 `pendingDraft`：
```typescript
const { paper, currentStep, loading, saving, dirty, pendingDraft, initNew, loadDraft, setStep, autoSave, reset } = usePaperEditorStore();
```

- [ ] **Step 2: 新建试卷路径加草稿检测**

在 `useEffect` 中（新建试卷路径），添加草稿检测：
```typescript
useEffect(() => {
    if (id) {
      loadDraft(id);
    } else {
      // 新建试卷：检查是否有未完成的草稿
      const checkDrafts = async () => {
        try {
          const { default: apiClient } = await import('../../api/client');
          const resp = await apiClient.get('/drafts');
          const drafts = Array.isArray(resp.data) ? resp.data : [];
          if (drafts.length > 0) {
            // 有待用户确认的草稿 — 简单起见，加载最近的一条
            const latest = drafts[0];
            // 设置 pendingDraft
            usePaperEditorStore.setState({ pendingDraft: latest.data });
          }
        } catch {}
      };
      checkDrafts();
      initNew();
    }
    ...
}, [id]);
```

- [ ] **Step 3: 添加草稿恢复 Modal**

在组件 JSX 中的 Steps 上方添加：
```tsx
{pendingDraft && (
    <Modal
      title="发现未完成的试卷"
      open={true}
      onOk={() => {
        // 从草稿恢复
        const draft = pendingDraft;
        usePaperEditorStore.setState({
          paper: draft,
          pendingDraft: null,
          currentStep: 0,
          dirty: false,
        });
        message.info('已恢复草稿');
      }}
      onCancel={() => {
        usePaperEditorStore.setState({ pendingDraft: null });
      }}
      okText="继续编辑"
      cancelText="重新开始"
    >
      <p>检测到上次未完成的试卷「{pendingDraft?.title || '未命名'}」，是否继续编辑？</p>
    </Modal>
  )}
```

在 import 中添加 `Modal`：
```typescript
import { Steps, Button, Space, message, Spin, Tooltip, Modal } from 'antd';
```

- [ ] **Step 4: TypeScript 编译检查**

```bash
cd /home/nvidia/Desktop/ruicheng-edu-system/frontend
npm run build 2>&1 | grep "PaperWizard" | head -5
```

- [ ] **Step 5: 提交**

```bash
git -C /home/nvidia/Desktop/ruicheng-edu-system add frontend/src/pages/papers/PaperWizardPage.tsx
git -C /home/nvidia/Desktop/ruicheng-edu-system commit -m "feat: 新建试卷检测草稿弹窗，加载草稿可恢复"
```

---

## 验证清单

1. 新建试卷 → 填写步骤 → 各步切换时 drafts 表有记录 → 关闭页面重开 → 弹窗"是否继续编辑"
2. 确认保存 → 主表 status=READY → 草稿表记录被清除
3. 确认保存并发布 → 主表 status=PUBLISHED
4. 编辑已有试卷 → 修改内容 → 保存 → 主表更新 → 草稿表无记录
