# V4.4 草稿去纸化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新建试卷在 Step 4 保存前不产生 exam_papers 主表记录，所有中间状态仅存草稿表 (paper_id=NULL)

**Architecture:** 新增 2 个 paperless API 端点（auto-generate, swap），前端 autoSave 去建表逻辑，saveAll 改为三阶段（建表→写数据→删草稿）

**Tech Stack:** FastAPI + React 19 + TypeScript + Zustand 5

---

### Task 1: Backend — Schema 变更

**Files:**
- Modify: `backend/app/schemas/exam_paper.py:114-118`
- Modify: `backend/app/schemas/exam_paper.py:149-155` (after AutoGenerateResponse)

- [ ] **Step 1: 在 AutoGenerateRequest 加 subject 字段**

```python
# backend/app/schemas/exam_paper.py:114-118 替换为:

class AutoGenerateRequest(BaseModel):
    difficulty_ratio: dict[str, float] = Field(default={"EASY": 0.3, "MEDIUM": 0.5, "HARD": 0.2})
    knowledge_node_ids: list[str] = Field(default=[])
    existing_question_ids: list[str] = Field(default=[])
    type_configs: list[dict] = Field(default=[])
    subject: Optional[str] = None  # V4.4: paperless 场景前端直传
```

- [ ] **Step 2: 新增 SwapQuestionRequest schema**

```python
# backend/app/schemas/exam_paper.py 在 AutoGenerateResponse 后添加:

class SwapQuestionRequest(BaseModel):
    """V4.4: paperless 换题请求，脱离 paper_id 依赖"""
    subject: Optional[str] = None
    grade_level: Optional[dict] = None
    knowledge_node_ids: list[str] = Field(default=[])
    exclude_ids: list[str] = Field(default=[])
```

- [ ] **Step 3: 验证后端启动无语法错误**

Run: `cd backend && python -c "from app.schemas.exam_paper import AutoGenerateRequest, SwapQuestionRequest; print('OK')"`

- [ ] **Step 4: Commit**

```bash
git add backend/app/schemas/exam_paper.py
git commit -m "feat: schema — AutoGenerateRequest 加 subject，新增 SwapQuestionRequest"
```

---

### Task 2: Backend — Paperless auto-generate 端点

**Files:**
- Modify: `backend/app/api/v1/endpoints/exam_papers.py:1259` (after existing auto_generate_paper)
- Modify: `backend/app/api/v1/endpoints/exam_papers.py:7` (import 加 Optional)

- [ ] **Step 1: 确保 typing.Optional 已导入**

```python
# backend/app/api/v1/endpoints/exam_papers.py 第 12 行已有:
from typing import Optional
# 确认存在即可，无需修改
```

- [ ] **Step 2: 确认 schemas 导入包含新类**

```python
# backend/app/api/v1/endpoints/exam_papers.py:27-36 替换 import 段为:
from app.schemas.exam_paper import (
    ExamPaperCreate,
    ExamPaperUpdate,
    ExamPaperUnitCreate,
    ExamPaperUnitUpdate,
    UnitQuestionCreate,
    ExamPaperFullSave,
    QuestionConfigItem,
    AutoGenerateRequest,
    SwapQuestionRequest,
)
```

- [ ] **Step 3: 在 auto_generate_paper 后面添加 paperless 端点**

在 `backend/app/api/v1/endpoints/exam_papers.py` 第 1259 行后插入:

```python


@router.post("/auto-generate")
async def auto_generate_paperless(
    request: AutoGenerateRequest,
    current_user = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
    """V4.4: 一键生成推荐 — paperless 版本，不依赖 paper_id"""
    _check_teacher_or_admin(current_user)

    # 1. Build type configs — paperless 场景必须前端直传
    if not request.type_configs:
        raise HTTPException(400, detail="请先在试卷结构步骤设置题型")
    type_configs = [{
        "question_type": cfg["question_type"],
        "count": cfg.get("count", 0),
        "score_per_question": cfg.get("score_per_question", 5),
    } for cfg in request.type_configs if cfg.get("count", 0) > 0]

    if not type_configs:
        raise HTTPException(400, detail="试卷结构中没有配置题型")

    # 2. Normalize difficulty ratio
    ratio = request.difficulty_ratio
    diffs = ["EASY", "MEDIUM", "HARD"]
    total_ratio = sum(ratio.get(d, 0) for d in diffs)
    if total_ratio <= 0:
        ratio = {"EASY": 0.3, "MEDIUM": 0.5, "HARD": 0.2}
    elif abs(total_ratio - 1.0) > 0.001:
        ratio = {d: ratio.get(d, 0) / total_ratio for d in diffs}

    # 3. Distribute quotas and select questions
    targets = distribute_quotas(type_configs, ratio)
    questions, dashboard = await select_for_targets(
        db, targets, set(request.knowledge_node_ids),
        request.subject,  # V4.4: 从 body 直取，不再查 DB paper
        pre_existing_ids=request.existing_question_ids,
    )

    return {"questions": questions, "constraint_dashboard": dashboard}
```

- [ ] **Step 4: 验证后端启动**

Run: `cd backend && timeout 5 python -c "from app.main import app; print('OK')" 2>&1 || true`

- [ ] **Step 5: Commit**

```bash
git add backend/app/api/v1/endpoints/exam_papers.py
git commit -m "feat: 新增 POST /exam-papers/auto-generate paperless 端点"
```

---

### Task 3: Backend — Paperless swap 端点

**Files:**
- Modify: `backend/app/api/v1/endpoints/questions.py:207` (在 deduplicate 之后)
- Modify: `backend/app/api/v1/endpoints/questions.py:1-5` (import 段)

- [ ] **Step 1: 更新 questions.py 的 import 段**

```python
# backend/app/api/v1/endpoints/questions.py:1-6 替换为:
import uuid
from fastapi import APIRouter, Depends, HTTPException, status, Body, Query
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select, update, delete, and_, or_, cast, String
from app.db.session import get_db
from app.models.question import Question
from app.schemas.exam_paper import SwapQuestionRequest
from app.schemas.question import QuestionCreate, QuestionResponse, QuestionUpdate
from typing import List, Optional
from app.core.security import get_current_user
import json as _json
from app.services.config_service import load_config
```

- [ ] **Step 2: 在 questions.py 的 deduplicate 端点后添加 swap 端点**

在 `backend/app/api/v1/endpoints/questions.py` 第 207 行 `@router.post("/deduplicate")` 之前插入:

```python


@router.post("/{question_id}/swap")
async def swap_question_paperless(
    question_id: str,
    request: SwapQuestionRequest,
    current_user = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
    """V4.4: 换题 — paperless 版本，逐级放宽约束链返回 top 3 备选题"""
    # 权限检查
    if current_user.user_type not in ("TEACHER", "QUESTION_ADMIN", "SYS_ADMIN"):
        raise HTTPException(status_code=status.HTTP_403_FORBIDDEN, detail="仅教师和题库管理员可换题")

    # 查当前题目
    result = await db.execute(select(Question).where(Question.id == question_id))
    current = result.scalar_one_or_none()
    if not current:
        raise HTTPException(404, detail="题目不存在")

    exclude_ids = set(request.exclude_ids)

    async def _query_candidates(qtype, diff, used, subj=None, grades_val=None, kn_ids=None):
        conds = [Question.is_active == True, Question.review_status == "APPROVED", Question.question_type == qtype]
        if diff:
            conds.append(Question.difficulty == diff)
        if subj:
            conds.append(Question.subject == subj)
        if grades_val:
            grade_conds = []
            for g in grades_val:
                grade_conds.append(cast(Question.grade_level["grades"].astext, String).contains(g))
            if grade_conds:
                conds.append(or_(*grade_conds))
        if kn_ids:
            try:
                from app.models.knowledge_node import QuestionKnowledgeNode
                kn_qids = await db.execute(
                    select(QuestionKnowledgeNode.question_id).where(
                        QuestionKnowledgeNode.knowledge_node_id.in_(kn_ids)
                    )
                )
                kn_qid_set = {str(r[0]) for r in kn_qids.fetchall()}
                if kn_qid_set:
                    conds.append(Question.id.in_(kn_qid_set))
            except Exception:
                pass
        r = await db.execute(select(Question).where(*conds))
        return [q for q in r.scalars().all() if str(q.id) not in used]

    subject = request.subject
    grades = request.grade_level.get("grades", []) if isinstance(request.grade_level, dict) else []
    kn_ids = request.knowledge_node_ids or []

    candidates = []
    # Level 1: 学科 + 年级 + 知识点
    if not candidates and subject and grades and kn_ids:
        candidates = await _query_candidates(
            current.question_type, current.difficulty, exclude_ids,
            subj=subject, grades_val=grades, kn_ids=kn_ids,
        )
    # Level 2: 学科 + 年级
    if not candidates and subject and grades:
        candidates = await _query_candidates(
            current.question_type, current.difficulty, exclude_ids,
            subj=subject, grades_val=grades,
        )
    # Level 3: 学科 + 题型 + 难度
    if not candidates:
        candidates = await _query_candidates(
            current.question_type, current.difficulty, exclude_ids,
            subj=subject,
        )

    if not candidates:
        return {"alternatives": []}

    alternatives = []
    for q in candidates[:3]:
        alternatives.append({
            "question_id": str(q.id),
            "title": (q.title or "")[:120],
            "difficulty": q.difficulty or "",
            "question_type": q.question_type or "",
            "score": q.score or 0,
        })
    return {"alternatives": alternatives}
```

- [ ] **Step 3: 验证后端启动**

Run: `cd backend && timeout 5 python -c "from app.main import app; print('OK')" 2>&1 || true`

- [ ] **Step 4: Commit**

```bash
git add backend/app/api/v1/endpoints/questions.py
git commit -m "feat: 新增 POST /questions/{qid}/swap paperless 端点"
```

---

### Task 4: Backend — 草稿 upsert 适配 paper_id=NULL

**Files:**
- Modify: `backend/app/api/v1/endpoints/drafts.py:29-34`

- [ ] **Step 1: 修改 save_draft 的查询逻辑**

```python
# backend/app/api/v1/endpoints/drafts.py:28-44 替换为:

    _check_teacher_or_admin(current_user)
    if body.paper_id:
        existing = await db.execute(
            select(ExamPaperDraft).where(
                ExamPaperDraft.user_id == current_user.id,
                ExamPaperDraft.paper_id == body.paper_id,
            )
        )
    else:
        existing = await db.execute(
            select(ExamPaperDraft).where(
                ExamPaperDraft.user_id == current_user.id,
                ExamPaperDraft.paper_id.is_(None),
            ).order_by(ExamPaperDraft.updated_at.desc()).limit(1)
        )
    draft = existing.scalar()
    if draft:
        draft.data = body.data
    else:
        draft = ExamPaperDraft(
            user_id=current_user.id,
            paper_id=body.paper_id,
            data=body.data,
        )
        db.add(draft)
    await db.commit()
    await db.refresh(draft)
    return draft
```

- [ ] **Step 2: 验证 Python 语法**

Run: `cd backend && python -c "from app.api.v1.endpoints.drafts import router; print('OK')"`

- [ ] **Step 3: Commit**

```bash
git add backend/app/api/v1/endpoints/drafts.py
git commit -m "fix: draft upsert 适配 paper_id=NULL 场景"
```

---

### Task 5: Frontend — API 方法 + Store 改动

**Files:**
- Modify: `frontend/src/api/papers.ts:53-54` (末尾)
- Modify: `frontend/src/store/paperEditor.ts:6-16` (State interface)
- Modify: `frontend/src/store/paperEditor.ts:59-73` (newEmptyPaper)
- Modify: `frontend/src/store/paperEditor.ts:115-126` (initNew/reset)
- Modify: `frontend/src/store/paperEditor.ts:401-503` (regenerateAll, fillGaps)
- Modify: `frontend/src/store/paperEditor.ts:581-648` (autoSave, saveAll)

- [ ] **Step 1: 在 papers.ts 末尾新增两个 paperless API 方法**

```typescript
// frontend/src/api/papers.ts 在 paperApi 对象末尾 (行 53 前) 添加:

  // V4.4 Paperless 端点
  autoGeneratePaperless: (data: {
    subject: string;
    difficulty_ratio: Record<string, number>;
    knowledge_node_ids: string[];
    existing_question_ids?: string[];
    type_configs: any[];
  }) => apiClient.post('/exam-papers/auto-generate', data),

  swapQuestionPaperless: (questionId: string, data: {
    subject?: string;
    grade_level?: any;
    knowledge_node_ids?: string[];
    exclude_ids?: string[];
  }) => apiClient.post(`/questions/${questionId}/swap`, data),
```

- [ ] **Step 2: 运行 TypeScript 编译验证**

Run: `cd frontend && npx tsc --noEmit --pretty false 2>&1 | head -20`

- [ ] **Step 3: Commit**

```bash
git add frontend/src/api/papers.ts
git commit -m "feat: 新增 paperless API 方法 autoGeneratePaperless / swapQuestionPaperless"
```

- [ ] **Step 4: Store State interface 加 draftId 字段**

```typescript
// frontend/src/store/paperEditor.ts:6-16 替换为:
interface PaperEditorState {
  // State
  paper: PaperDraft | null;
  currentStep: number;
  loading: boolean;
  saving: boolean;
  lastSaved: Date | null;
  dirty: boolean;
  autoSelectReports: AutoSelectReport[];
  generateReport: GenerateReport | null;
  pendingDraft: any | null;
  draftId: string | null;  // V4.4: 草稿记录 ID，用于精确删除
```

- [ ] **Step 5: newEmptyPaper 和 initNew/reset 加 draftId 初始值**

```typescript
// frontend/src/store/paperEditor.ts:126 替换 initNew:
  initNew: () => set({ paper: newEmptyPaper(), currentStep: 0, dirty: false, lastSaved: null, autoSelectReports: [], generateReport: null, pendingDraft: null, draftId: null }),

// frontend/src/store/paperEditor.ts:648 替换 reset:
  reset: () => set({ paper: null, currentStep: 0, dirty: false, lastSaved: null, autoSelectReports: [], generateReport: null, pendingDraft: null, draftId: null }),
```

- [ ] **Step 6: 替换 autoSave — 去建表逻辑，记录 draftId**

```typescript
// frontend/src/store/paperEditor.ts:581-607 替换为:
  autoSave: async () => {
    const { paper, dirty } = get();
    if (!paper || !dirty) return;
    set({ saving: true });
    try {
      const resp = await draftApi.save(paper.id || null, paper);
      const respData = resp?.data;
      const newDraftId = typeof respData === 'object' && respData !== null ? (respData as any).id : undefined;
      set({ saving: false, dirty: false, lastSaved: new Date(), draftId: newDraftId || null });
    } catch {
      set({ saving: false });
    }
  },
```

- [ ] **Step 7: 替换 regenerateAll — 改用 paperless 端点**

```typescript
// frontend/src/store/paperEditor.ts:401-443 替换为:
  regenerateAll: async () => {
    set({ loading: true });
    try {
      const paper = get().paper;
      if (!paper) return;
      const resp = await paperApi.autoGeneratePaperless({
        subject: paper.subject,
        difficulty_ratio: paper.difficulty_ratio || { EASY: 20, MEDIUM: 50, HARD: 30 },
        knowledge_node_ids: paper.knowledge_node_ids || [],
        existing_question_ids: [],
        type_configs: collectTypeConfigs(paper.units),
      });
      const data = resp.data;
      get().clearAllQuestions();
      (data.questions || []).forEach((rec: any) => {
        const unit = paper.units.find(u =>
          (u.question_config || []).some(c => c.question_type === rec.question_type)
        );
        if (unit?.id) {
          get().addQuestionToUnit(unit.id, {
            question_id: rec.question_id,
            question_type: rec.question_type,
            position: (unit.questions?.length || 0) + 1,
            score: rec.score,
            question: {
              id: rec.question_id,
              title: rec.title,
              question_type: rec.question_type,
              difficulty: rec.difficulty,
              subject: paper.subject,
              options: normalizeOpts((() => { try { return JSON.parse(rec.correct_answer || '{}').options; } catch { return []; } })()),
              correct_answer: rec.correct_answer || '',
            },
            recommendation_tags: rec.recommendation_tags || [],
            alternatives: rec.alternatives || [],
          });
        }
      });
      set({ generateReport: { constraint_dashboard: data.constraint_dashboard || {} } });
      set({ dirty: true });
    } finally {
      set({ loading: false });
    }
  },
```

- [ ] **Step 8: 替换 fillGaps — 改用 paperless 端点**

```typescript
// frontend/src/store/paperEditor.ts:445-503 替换为:
  fillGaps: async () => {
    const { paper } = get();
    if (!paper) return;
    const allExistingIds: string[] = [];
    const gaps: { unitId: string; questionType: string; deficit: number }[] = [];
    paper.units.forEach(u => {
      (u.question_config || []).forEach(cfg => {
        const existing = (u.questions || []).filter(q => q.question_type === cfg.question_type);
        existing.forEach(q => allExistingIds.push(q.question_id));
        if (cfg.count > existing.length) {
          gaps.push({ unitId: u.id || '', questionType: cfg.question_type, deficit: cfg.count - existing.length });
        }
      });
    });
    if (gaps.length === 0) return;

    set({ loading: true });
    try {
      const resp = await paperApi.autoGeneratePaperless({
        subject: paper.subject,
        difficulty_ratio: paper.difficulty_ratio || { EASY: 20, MEDIUM: 50, HARD: 30 },
        knowledge_node_ids: paper.knowledge_node_ids || [],
        existing_question_ids: allExistingIds,
        type_configs: collectTypeConfigs(paper.units),
      });
      const data = resp.data;
      const gapTypes = new Set(gaps.map(g => g.questionType));
      const newQuestions = (data.questions || []).filter((rec: any) => gapTypes.has(rec.question_type));
      gaps.forEach(gap => {
        const typeQuestions = newQuestions.filter((q: any) => q.question_type === gap.questionType);
        const toAdd = typeQuestions.slice(0, gap.deficit);
        toAdd.forEach((rec: any) => {
          const unit = paper.units.find(u => u.id === gap.unitId);
          if (unit) {
            get().addQuestionToUnit(gap.unitId, {
              question_id: rec.question_id,
              question_type: rec.question_type,
              position: (unit.questions?.length || 0) + (toAdd.indexOf(rec) + 1),
              score: rec.score,
              question: {
                id: rec.question_id,
                title: rec.title,
                question_type: rec.question_type,
                difficulty: rec.difficulty,
                subject: paper.subject,
                options: normalizeOpts((() => { try { return JSON.parse(rec.correct_answer || '{}').options; } catch { return []; } })()),
                correct_answer: rec.correct_answer || '',
              },
              recommendation_tags: rec.recommendation_tags || [],
              alternatives: rec.alternatives || [],
            });
          }
        });
      });
      set({ generateReport: { constraint_dashboard: data.constraint_dashboard || {} } });
      set({ dirty: true });
    } finally {
      set({ loading: false });
    }
  },
```

- [ ] **Step 9: 替换 saveAll — 三阶段：建表→写数据→删草稿**

```typescript
// frontend/src/store/paperEditor.ts:609-648 替换为:
  saveAll: async () => {
    const { paper, draftId } = get();
    if (!paper) return;
    set({ saving: true });
    try {
      // 阶段 1：新建时先创建主表记录拿 id
      let pid = paper.id;
      if (!pid) {
        const gl = (paper.grade_level && paper.grade_level.grades?.length > 0)
          ? paper.grade_level
          : { scope: 'grade', grades: ['G8'] };
        const resp = await paperApi.create({
          title: paper.title || '未命名试卷',
          subject: paper.subject || '',
          grade_level: gl,
          status: 'READY',
        });
        pid = resp.data?.id || resp.data;
        set({ paper: { ...get().paper, id: pid } });
      }

      // 阶段 2：写主表
      const cleanPaper = {
        ...paper,
        units: paper.units.map(u => ({
          name: u.name,
          description: u.description,
          position: u.position,
          time_limit_minutes: u.time_limit_minutes,
          question_config: u.question_config,
          questions: (u.questions || []).map(q => ({
            question_id: q.question_id,
            question_type: q.question_type,
            position: q.position,
            score: q.score,
          })),
        })),
      };
      await paperApi.saveAll(pid, cleanPaper);

      // 阶段 3：按 draftId 精确删除草稿，兜底按 paper_id
      if (draftId) {
        await draftApi.delete(draftId).catch(() => {});
      } else {
        try {
          const draftsResp = await draftApi.getByPaper(pid);
          const drafts = Array.isArray(draftsResp?.data) ? draftsResp.data : [];
          for (const d of drafts) await draftApi.delete(d.id).catch(() => {});
        } catch {}
      }

      set({ saving: false, dirty: false, lastSaved: new Date(), draftId: null });
    } catch (e) {
      set({ saving: false });
      throw e;
    }
  },
```

- [ ] **Step 10: 更新 interface 中 regenerateAll 和 fillGaps 的类型签名**

```typescript
// frontend/src/store/paperEditor.ts:52-53 替换为:
  regenerateAll: () => Promise<void>;
  fillGaps: () => Promise<void>;
```

- [ ] **Step 11: 运行 TypeScript 编译验证**

Run: `cd frontend && npx tsc --noEmit --pretty false 2>&1 | head -30`

- [ ] **Step 12: Commit**

```bash
git add frontend/src/store/paperEditor.ts
git commit -m "feat: store — autoSave/saveAll/regenerateAll/fillGaps 适配 paperless 架构"
```

---

### Task 6: Frontend — RecommendStep 改动

**Files:**
- Modify: `frontend/src/pages/papers/steps/RecommendStep.tsx:24-63` (useEffect)
- Modify: `frontend/src/pages/papers/steps/RecommendStep.tsx:138-180` (handleSwap)
- Modify: `frontend/src/pages/papers/steps/RecommendStep.tsx:257-286` (一键选题按钮)

- [ ] **Step 1: useEffect 去掉 `paper?.id` 守卫**

```typescript
// frontend/src/pages/papers/steps/RecommendStep.tsx:24-26 替换:
// 原: if (!paper?.id || hasAutoAdjusted.current) return;
// 改为:
    if (hasAutoAdjusted.current) return;
```

同时更新依赖:

```typescript
// frontend/src/pages/papers/steps/RecommendStep.tsx:63
// 原: }, [paper?.id]);
// 改为:
  }, [paper?.units]);
```

- [ ] **Step 2: handleSwap 改用 paperless 端点**

```typescript
// frontend/src/pages/papers/steps/RecommendStep.tsx:138-180 替换 handleSwap:
  const handleSwap = async (questionId: string, unitId: string) => {
    setSwapLoading(prev => ({ ...prev, [questionId]: true }));
    try {
      const st = usePaperEditorStore.getState();
      const p = st.paper;
      const allQids = (p?.units || []).flatMap(u =>
        (u.questions || []).map(q => q.question_id)
      );
      const resp = await paperApi.swapQuestionPaperless(questionId, {
        subject: p?.subject,
        grade_level: p?.grade_level,
        knowledge_node_ids: p?.knowledge_node_ids,
        exclude_ids: allQids,
      });
      const alts: any[] = resp.data?.alternatives || [];
      if (alts.length === 0) { message.warning('没有可替换的题目'); return; }
      const alt = alts[0];
      const remainingAlts: AlternativeQuestion[] = alts.slice(1).map((a: any) => ({
        question_id: a.question_id,
        title: a.title || '',
        difficulty: a.difficulty || '',
        tags: a.tags || [],
      }));
      const unit = units.find(u => u.id === unitId);
      const oldQ = unit?.questions?.find(q => q.question_id === questionId);
      const cfg = unit?.question_config?.find(c => c.question_type === alt.question_type);
      const swapScore = cfg?.score_per_question || alt.score || 5;
      replaceQuestion(unitId, questionId, {
        question_id: alt.question_id,
        question_type: alt.question_type,
        position: oldQ?.position || 0,
        score: swapScore,
        question: {
          id: alt.question_id,
          title: alt.title,
          question_type: alt.question_type,
          difficulty: alt.difficulty,
          subject: p?.subject,
        },
        recommendation_tags: ['已替换'],
        alternatives: remainingAlts,
      });
      setDirty(true);
      message.success('已替换');
    } catch {
      message.error('换题失败');
    } finally {
      setSwapLoading(prev => ({ ...prev, [questionId]: false }));
    }
  };
```

- [ ] **Step 3: "一键选题" 按钮去掉建表兜底**

```typescript
// frontend/src/pages/papers/steps/RecommendStep.tsx:257-286 替换 onClick:
          onClick={async () => {
            setLoading(true);
            clearAllQuestions();
            const st = usePaperEditorStore.getState();
            st.regenerateAll().catch((e: any) => {
              const detail = e?.response?.data?.detail || e?.message || '选题失败';
              message.error(typeof detail === 'string' ? detail : JSON.stringify(detail));
            }).finally(() => setLoading(false));
          }}
```

- [ ] **Step 4: 更新 useEffect 中的 regenerateAll/fillGaps 调用（去掉 paperId 参数）**

```typescript
// frontend/src/pages/papers/steps/RecommendStep.tsx:44 附近
// 原: fillGaps(paper.id).then(...)
// 改为:
        fillGaps().then(() => {
// 原: regenerateAll(paper.id).then(...)
// 改为:
        regenerateAll().then(() => {
```

- [ ] **Step 5: 运行 TypeScript 编译验证**

Run: `cd frontend && npx tsc --noEmit --pretty false 2>&1 | head -30`

- [ ] **Step 6: Commit**

```bash
git add frontend/src/pages/papers/steps/RecommendStep.tsx
git commit -m "feat: RecommendStep 适配 paperless — 去 paper.id 依赖，换用 getState()"
```

---

### Task 7: Frontend — PreviewFinalizeStep 改动

**Files:**
- Modify: `frontend/src/pages/papers/steps/PreviewFinalizeStep.tsx:26-56` (handleSave, handleSaveAndPublish)

- [ ] **Step 1: 替换 handleSave — 去守卫，由 saveAll 内部处理**

```typescript
// frontend/src/pages/papers/steps/PreviewFinalizeStep.tsx:26-38 替换为:
  const handleSave = async () => {
    setSubmitting(true);
    try {
      await saveAll();
      message.success('已保存');
      usePaperEditorStore.getState().setDirty(false);
      setTimeout(() => navigate('/papers'), 500);
    } catch (err: any) {
      const detail = err?.response?.data?.detail;
      message.error(typeof detail === 'string' ? detail : '保存失败');
    } finally { setSubmitting(false); }
  };
```

- [ ] **Step 2: 替换 handleSaveAndPublish — 用 getState() 取最新 paper.id**

```typescript
// frontend/src/pages/papers/steps/PreviewFinalizeStep.tsx:40-56 替换为:
  const handleSaveAndPublish = async () => {
    setSubmitting(true);
    try {
      await saveAll();
      const pid = usePaperEditorStore.getState().paper?.id;
      if (pid) {
        await apiClient.post(`/exam-papers/${pid}/publish`, {
          class_ids: selectedClasses,
          note: publishNote || undefined,
        });
      }
      message.success('已保存并发布');
      usePaperEditorStore.getState().setDirty(false);
      setTimeout(() => navigate('/papers'), 500);
    } catch (err: any) {
      const detail = err?.response?.data?.detail;
      message.error(typeof detail === 'string' ? detail : '保存失败');
    } finally { setSubmitting(false); }
  };
```

- [ ] **Step 3: 运行 TypeScript 编译验证**

Run: `cd frontend && npx tsc --noEmit --pretty false 2>&1 | head -30`

- [ ] **Step 4: Commit**

```bash
git add frontend/src/pages/papers/steps/PreviewFinalizeStep.tsx
git commit -m "feat: PreviewFinalizeStep 适配 paperless — saveAll 内部建表，getState() 防闭包"
```

---

### 验证清单

- [ ] 后端启动无错误
- [ ] `POST /api/v1/exam-papers/auto-generate` (paperless) 返回推荐结果
- [ ] `POST /api/v1/questions/{qid}/swap` (paperless) 返回备选题
- [ ] 新建流程：Step 0→1→2 不产生 exam_papers 记录
- [ ] 新建流程：RecommendStep 正常自动选题（无 paper.id 时）
- [ ] 新建流程：换题功能正常
- [ ] 新建流程：Step 4 保存后产生完整 exam_papers 记录
- [ ] 新建流程：保存后草稿被清理
- [ ] 编辑流程：全部功能不受影响
- [ ] 中断恢复：新建中途关闭浏览器，再次新建可恢复草稿
- [ ] `npm run build` 通过
