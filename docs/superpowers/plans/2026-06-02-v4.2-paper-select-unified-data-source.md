# V4.2 选题数据源统一 + 结构调整联动 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `unit.questions` 作为唯一选题数据源，删除 generateReport.questions；结构调整后自动检测缺口补题、多余全部标红；一键选题全量替换；换题去重。

**Architecture:** 后端 `select_for_targets` 加 `pre_existing_ids` 参数排除已有题目；前端 `ExamPaperUnitQuestion` 扩展 `recommendation_tags`/`alternatives` 字段；store 新增 `regenerateAll`/`fillGaps` 两个 async action；RecommendStep 数据源从 `generateReport.questions` 切换为 `paper.units.questions`。

**Tech Stack:** FastAPI + React 19 + TypeScript + Zustand 5

---

### Task 1: 后端 — existing_question_ids 去重参数

**Files:**
- Modify: `backend/app/schemas/exam_paper.py:114-116`
- Modify: `backend/app/services/recommendation_engine.py:129-135`
- Modify: `backend/app/api/v1/endpoints/exam_papers.py:1166-1222`

- [ ] **Step 1: AutoGenerateRequest 加 existing_question_ids 字段**

```python
# backend/app/schemas/exam_paper.py 替换第 114-116 行
class AutoGenerateRequest(BaseModel):
    difficulty_ratio: dict[str, float] = Field(default={"EASY": 0.3, "MEDIUM": 0.5, "HARD": 0.2})
    knowledge_node_ids: list[str] = Field(default=[])
    existing_question_ids: list[str] = Field(default=[])
```

- [ ] **Step 2: select_for_targets 加 pre_existing_ids 参数**

```python
# backend/app/services/recommendation_engine.py 替换第 129-135 行函数签名
async def select_for_targets(
    db: AsyncSession,
    targets: list[QuotaTarget],
    knowledge_node_ids: set[str],
    subject: Optional[str] = None,
    pre_existing_ids: Optional[list[str]] = None,
) -> tuple[list[dict], dict]:
    """为所有目标选题，返回题目列表和约束仪表盘数据.

    pre_existing_ids: 已存在于试卷中的 question_id 列表，确保去重.
    """
    used_ids: set[str] = set(pre_existing_ids or [])
    # ... 后续逻辑不变 (used_ids 从已有 ID 起始)
```

- [ ] **Step 3: auto_generate_paper 端点传入 existing_question_ids**

```python
# backend/app/api/v1/endpoints/exam_papers.py 替换第 1217-1219 行
    targets = distribute_quotas(type_configs, ratio)
    questions, dashboard = await select_for_targets(
        db, targets, set(request.knowledge_node_ids), paper.subject,
        pre_existing_ids=request.existing_question_ids,
    )
```

- [ ] **Step 4: 运行后端测试验证**

```bash
cd /home/nvidia/Desktop/ruicheng-edu-system/backend
conda activate ~/conda_workspace
python -c "
from app.schemas.exam_paper import AutoGenerateRequest
r = AutoGenerateRequest(difficulty_ratio={'EASY': 0.3, 'MEDIUM': 0.5, 'HARD': 0.2}, knowledge_node_ids=[], existing_question_ids=['a', 'b'])
assert r.existing_question_ids == ['a', 'b']
print('OK')
"
```

- [ ] **Step 5: 提交**

```bash
cd /home/nvidia/Desktop/ruicheng-edu-system
git add backend/app/schemas/exam_paper.py backend/app/services/recommendation_engine.py backend/app/api/v1/endpoints/exam_papers.py
git commit -m "feat: auto-generate 加 existing_question_ids 参数支持增量补题去重"
```

---

### Task 2: 前端类型 — ExamPaperUnitQuestion 扩展 + GenerateReport 精简

**Files:**
- Modify: `frontend/src/types/paper.ts:26-35,101-135`

- [ ] **Step 1: 扩展 ExamPaperUnitQuestion 类型**

```typescript
// frontend/src/types/paper.ts 替换第 26-35 行
export interface ExamPaperUnitQuestion {
  id?: string;
  unit_id?: string;
  question_id: string;
  question_type: string;
  position: number;
  score: number;
  // joined question data
  question?: QuestionBrief;
  // V4.2: 推荐元数据（直接从后端 auto-generate/swap 响应写入）
  recommendation_tags?: string[];
  alternatives?: AlternativeQuestion[];
}
```

- [ ] **Step 2: 精简 GenerateReport，去 questions 字段**

```typescript
// frontend/src/types/paper.ts 替换第 132-135 行
export interface GenerateReport {
  constraint_dashboard: ConstraintDashboard;
}
```

- [ ] **Step 3: 删除 GenerateRecommendation 类型（不再需要）**

```typescript
// frontend/src/types/paper.ts 删除第 101-109 行 (GenerateRecommendation 接口)
// 保留 AlternativeQuestion (line 111-116) 和 ConstraintDashboard (line 124-130)
// 注意：AlternativeQuestion 已被 ExamPaperUnitQuestion.alternatives 引用，保留
```

- [ ] **Step 4: TypeScript 编译检查**

```bash
cd /home/nvidia/Desktop/ruicheng-edu-system/frontend
npx tsc --noEmit 2>&1 | head -30
```

> 预期：有类型错误（RecommendStep 仍引用已删除的类型），下一任务修复。

- [ ] **Step 5: 提交**

```bash
cd /home/nvidia/Desktop/ruicheng-edu-system
git add frontend/src/types/paper.ts
git commit -m "refactor: ExamPaperUnitQuestion 扩展推荐元数据，GenerateReport 精简去 questions"
```

---

### Task 3: 前端 Store — 新增 regenerateAll / fillGaps / setUnitQuestions

**Files:**
- Modify: `frontend/src/store/paperEditor.ts`
- Modify: `frontend/src/api/papers.ts` (autoGenerate 调用加参数)

- [ ] **Step 1: autoGenerate API 函数签名更新**

```typescript
// frontend/src/api/papers.ts 替换第 37-40 行
autoGenerate: (paperId: string, data: {
    difficulty_ratio: { EASY: number; MEDIUM: number; HARD: number };
    knowledge_node_ids: string[];
    existing_question_ids?: string[];
  }) => apiClient.post(`/exam-papers/${paperId}/auto-generate`, data),
```

- [ ] **Step 2: Store 新增 setUnitQuestions action**

在 `store/paperEditor.ts` 的 `clearAllQuestions` 之后添加：

```typescript
setUnitQuestions: (uid: string, questions: ExamPaperUnitQuestion[]) => {
  const { paper } = get();
  if (!paper) return;
  set({
    paper: {
      ...paper,
      units: paper.units.map((u) =>
        u.id === uid ? { ...u, questions } : u
      ),
    },
    dirty: true,
  });
},
```

- [ ] **Step 3: Store 新增 regenerateAll action**

在 `setUnitQuestions` 之后添加：

```typescript
regenerateAll: async (paperId: string) => {
  set({ loading: true });
  try {
    const resp = await paperApi.autoGenerate(paperId, {
      difficulty_ratio: get().paper?.difficulty_ratio || { EASY: 20, MEDIUM: 50, HARD: 30 },
      knowledge_node_ids: get().paper?.knowledge_node_ids || [],
      existing_question_ids: [],  // 全量替换，不排除
    });
    const data = resp.data;
    // 清空所有 unit questions
    get().clearAllQuestions();
    // 按 question_type 分配题目到对应 unit
    const paper = get().paper;
    if (!paper) return;
    (data.questions || []).forEach((rec: any) => {
      const unit = paper.units.find(u =>
        (u.question_config || []).some(c => c.question_type === rec.question_type)
      );
      if (unit?.id) {
        get().addQuestionToUnit(unit.id, {
          question_id: rec.question_id,
          question_type: rec.question_type,
          position: (unit.questions?.length || 0) + 1,
          score: rec.score,
          question: {
            id: rec.question_id,
            title: rec.title,
            question_type: rec.question_type,
            difficulty: rec.difficulty,
            subject: paper.subject,
          },
          recommendation_tags: rec.recommendation_tags || [],
          alternatives: rec.alternatives || [],
        });
      }
    });
    set({ generateReport: { constraint_dashboard: data.constraint_dashboard || {} } });
  } finally {
    set({ loading: false });
  }
},
```

- [ ] **Step 4: Store 新增 fillGaps action**

在 `regenerateAll` 之后添加：

```typescript
fillGaps: async (paperId: string) => {
  const { paper } = get();
  if (!paper) return;
  // 收集所有已有 question_id 和缺口统计
  const allExistingIds: string[] = [];
  const gaps: { unitId: string; questionType: string; deficit: number }[] = [];
  paper.units.forEach(u => {
    (u.question_config || []).forEach(cfg => {
      const existing = (u.questions || []).filter(q => q.question_type === cfg.question_type);
      existing.forEach(q => allExistingIds.push(q.question_id));
      if (cfg.count > existing.length) {
        gaps.push({ unitId: u.id || '', questionType: cfg.question_type, deficit: cfg.count - existing.length });
      }
    });
  });
  if (gaps.length === 0) return;

  set({ loading: true });
  try {
    const resp = await paperApi.autoGenerate(paperId, {
      difficulty_ratio: paper.difficulty_ratio || { EASY: 20, MEDIUM: 50, HARD: 30 },
      knowledge_node_ids: paper.knowledge_node_ids || [],
      existing_question_ids: allExistingIds,
    });
    const data = resp.data;
    // 只取缺口题型，按 deficit 数量截取
    const gapTypes = new Set(gaps.map(g => g.questionType));
    const newQuestions = (data.questions || []).filter((rec: any) => gapTypes.has(rec.question_type));
    // 按题型分配
    gaps.forEach(gap => {
      const unit = paper.units.find(u => u.id === gap.unitId);
      if (!unit) return;
      const typeQuestions = newQuestions.filter((q: any) => q.question_type === gap.questionType);
      // 仅取需要的数量
      const toAdd = typeQuestions.slice(0, gap.deficit);
      toAdd.forEach((rec: any) => {
        get().addQuestionToUnit(gap.unitId, {
          question_id: rec.question_id,
          question_type: rec.question_type,
          position: (get().paper?.units.find(u => u.id === gap.unitId)?.questions?.length || 0) + 1,
          score: rec.score,
          question: {
            id: rec.question_id,
            title: rec.title,
            question_type: rec.question_type,
            difficulty: rec.difficulty,
            subject: paper.subject,
          },
          recommendation_tags: rec.recommendation_tags || [],
          alternatives: rec.alternatives || [],
        });
      });
    });
    // 更新 dashboard
    const updatedDashboard = {
      ...data.constraint_dashboard,
      // 合并已有题目和新题目的难度统计
    };
    set({ generateReport: { constraint_dashboard: updatedDashboard } });
  } finally {
    set({ loading: false });
  }
},
```

- [ ] **Step 5: setGenerateReport 简化**

替换原有实现（仅保留 constraint_dashboard）：

```typescript
// store/paperEditor.ts 替换 setGenerateReport
setGenerateReport: (report) => set({ generateReport: report ? { constraint_dashboard: report.constraint_dashboard } : null }),
```

- [ ] **Step 6: TypeScript 编译检查**

```bash
cd /home/nvidia/Desktop/ruicheng-edu-system/frontend
npx tsc --noEmit 2>&1 | head -30
```

> 预期：RecommendStep 仍有类型错误（下一任务修复），store 无新增错误。

- [ ] **Step 7: 提交**

```bash
cd /home/nvidia/Desktop/ruicheng-edu-system
git add frontend/src/store/paperEditor.ts frontend/src/api/papers.ts
git commit -m "feat: store 新增 regenerateAll/fillGaps/setUnitQuestions，GenerateReport 精简"
```

---

### Task 4: RecommendStep 重构 — 数据源切换 + 多余标红 + 删除修复 + 一键选题

**Files:**
- Modify: `frontend/src/pages/papers/steps/RecommendStep.tsx`

- [ ] **Step 1: 重写 import 和类型引用**

移除 `GenerateRecommendation` 导入，改用 `ExamPaperUnitQuestion`：

```typescript
// 替换第 6 行
import type { ExamPaperUnitQuestion } from '../../../types/paper';
```

- [ ] **Step 2: 重写数据源 — groupByType 从 paper.units 读取**

替换整个 `groupedByType` 计算（原第 276-279 行）：

```typescript
const groupedByType = useMemo(() => {
  const groups: Record<string, ExamPaperUnitQuestion[]> = {};
  (paper?.units || []).forEach(unit => {
    (unit.questions || []).forEach(q => {
      const qt = q.question_type || 'SINGLE_CHOICE';
      if (!groups[qt]) groups[qt] = [];
      groups[qt].push(q);
    });
  });
  return groups;
}, [paper?.units]);
```

- [ ] **Step 3: 重写 auto-generate 响应处理 — 写入 unit.questions 而非 generateReport**

替换 `handleGenerate` 函数（原第 128-168 行）为同步 store 的 `regenerateAll`：

```typescript
const handleGenerate = async (_force = false) => {
  if (!paper?.id) { message.warning('请先保存基本信息'); return; }
  await regenerateAll(paper.id);
  message.success('已重新生成题目');
};
```

- [ ] **Step 4: 重写进入 Step3 时的增量调整逻辑**

替换 `useEffect`（原第 23-42 行）：

```typescript
const hasAutoAdjusted = useRef(false);
useEffect(() => {
  if (!paper?.id || hasAutoAdjusted.current) return;
  const units = paper?.units || [];
  const hasExistingQuestions = units.some(u => (u.questions || []).length > 0);
  if (hasExistingQuestions) {
    // 已有题目：检测缺口 + 标记多余
    hasAutoAdjusted.current = true;
    const hasGaps = units.some(u =>
      (u.question_config || []).some(cfg => {
        const existing = (u.questions || []).filter(q => q.question_type === cfg.question_type).length;
        return cfg.count > existing;
      })
    );
    if (hasGaps) {
      fillGaps(paper.id).then(() => {
        message.info('已自动补充缺口题目');
      });
    }
  } else {
    // 无题目：自动全量生成
    hasAutoAdjusted.current = true;
    const hasConfigs = units.some(u => (u.question_config || []).some(c => (c.count || 0) > 0));
    if (hasConfigs) {
      regenerateAll(paper.id).then(() => {
        message.success('已自动生成题目');
      });
    }
  }
  // eslint-disable-next-line react-hooks/exhaustive-deps
}, [paper?.id]);
```

- [ ] **Step 5: 重写删除处理 — 同时清理 alternatives 引用（仅需从 unit.questions 删除即生效）**

`handleRemove` 保持不变（调用 `removeQuestionFromUnit`），因为渲染已从同一数据源读取。

- [ ] **Step 6: 重写换题处理 — 将 recommend_tags/alternatives 写入替换后的 question**

替换 `handleSwap` 中 `generateReport` 同步部分（原第 194-215 行）：

```typescript
// handleSwap 中移除 generateReport 同步代码块 (原第 194-215 行整个 if 块)
// 改为通过 replaceQuestion 同时写入 tags/alternatives
removeQuestionFromUnit(unitId, questionId);
addQuestionToUnit(unitId, {
  question_id: alt.question_id,
  question_type: alt.question_type,
  position: (units.find(u => u.id === unitId)?.questions?.length || 0) + 1,
  score: alt.score || 5,
  question: {
    id: alt.question_id,
    title: alt.title,
    question_type: alt.question_type,
    difficulty: alt.difficulty,
    subject: paper.subject,
  },
  recommendation_tags: ['已替换'],
  alternatives: remainingAlts,
});
```

- [ ] **Step 7: 重写手工选题替换 — 同步写入 tags**

替换 `handleManualReplace` 中 `generateReport` 同步部分（原第 109-120 行整个 if 块）：改为仅操作 unit.questions（add/remove），加上 `recommendation_tags: ['手工选择']`。

- [ ] **Step 8: 重写多余标记逻辑 — 整题型标记**

替换渲染中的 `isExtra` 判断（原第 321 行）：

```typescript
// 该题型整体是否超量
const excessCount = questions.length - configCount;
const isExcessType = excessCount > 0;
// 每道题都标记 isExcessType（不再只标记尾部）
```

在题目卡片上应用异常样式时（原第 329-330 行），条件改为 `isExcessType`：

```typescript
borderLeft: isExcessType ? '3px solid #ff4d4f' : 'none',
paddingLeft: isExcessType ? 8 : 0,
```

每道题的"多余"Tag 也改为 `isExcessType`（原第 339 行）：

```typescript
{isExcessType && <Tag color="error" style={{ fontSize: 10, marginRight: 4 }}>多余</Tag>}
```

- [ ] **Step 9: 重写一键选题按钮**

替换按钮行为（原第 292-299 行）：

```typescript
<Button type="primary" icon={<SyncOutlined />} loading={loading} onClick={() => {
  if (!paper?.id) { message.warning('请先保存基本信息'); return; }
  clearAllQuestions();
  regenerateAll(paper.id);
}}>
  一键选题
</Button>
```

- [ ] **Step 10: 重写头部计数显示 — 从 unit.questions 计算**

```typescript
// 替换第 286-291 行
const totalQCount = (paper?.units || []).reduce((s, u) => s + (u.questions?.length || 0), 0);
const hasQuestions = totalQCount > 0;
// ...
{hasQuestions ? (
  <span style={{ fontSize: 13, color: '#666' }}>共 {totalQCount} 题</span>
) : (
  <span style={{ fontSize: 13, color: '#999' }}>点击"一键选题"自动选题</span>
)}
```

- [ ] **Step 11: 仪表盘数据来源 — constraint_dashboard**

`dashboard` 变量从 `generateReport?.constraint_dashboard` 读取（不是 `generateReport?.constraint_dashboard`），字段访问不变。需修改第 232 行：

```typescript
const dashboard = generateReport?.constraint_dashboard;
```

- [ ] **Step 12: TypeScript 编译检查**

```bash
cd /home/nvidia/Desktop/ruicheng-edu-system/frontend
npx tsc --noEmit 2>&1 | head -30
```

> 预期：0 错误。

- [ ] **Step 13: 提交**

```bash
cd /home/nvidia/Desktop/ruicheng-edu-system
git add frontend/src/pages/papers/steps/RecommendStep.tsx
git commit -m "refactor: RecommendStep 数据源切换为 unit.questions，多余全部标记，删除即生效"
```

---

### Task 5: 导入清理 + 端到端验证

**Files:**
- Modify: `frontend/src/pages/papers/steps/RecommendStep.tsx` (import cleanup)
- Modify: `frontend/src/store/paperEditor.ts` (移除未使用的 import)

- [ ] **Step 1: 清理 RecommendStep 中未使用的 import**

移除 `GenerateRecommendation` 导入（已在 Task 4 Step 1 处理），确认所有 `generateReport.questions` 引用已清除：

```bash
cd /home/nvidia/Desktop/ruicheng-edu-system/frontend
grep -n "generateReport\.questions\|GenerateRecommendation" src/pages/papers/steps/RecommendStep.tsx
```

> 预期：无输出。

- [ ] **Step 2: 清理 store 中可能的旧类型引用**

确认 `paperEditor.ts` 中不再引用 `GenerateRecommendation`：

```bash
grep -n "GenerateRecommendation" src/store/paperEditor.ts
```

> 预期：无输出。若有，替换为具体类型。

- [ ] **Step 3: 前端编译验证**

```bash
cd /home/nvidia/Desktop/ruicheng-edu-system/frontend
npx tsc --noEmit
```

> 预期：0 错误。

- [ ] **Step 4: 后端测试验证**

```bash
cd /home/nvidia/Desktop/ruicheng-edu-system/backend
conda activate ~/conda_workspace
python -c "
from app.services.recommendation_engine import select_for_targets, QuotaTarget, distribute_quotas
# 语法导入检查
print('All imports OK')
"
```

- [ ] **Step 5: 提交**

```bash
cd /home/nvidia/Desktop/ruicheng-edu-system
git add frontend/src/pages/papers/steps/RecommendStep.tsx frontend/src/store/paperEditor.ts
git commit -m "chore: 清理 GenerateRecommendation 残留引用"
```

---

## 验证清单

完成所有任务后，手动验证以下流程：

1. 新建试卷 → Step2 设置题数 → Step3 自动生成 → 题目显示正常
2. 回到 Step2 减少题数 → Step3 → 该题型所有题目标红 + 多余 Tag → 点击删除可用
3. 回到 Step2 增加题数 → Step3 → 自动补缺口
4. 点击"一键选题" → 全部清空重新生成
5. 点击某题的"换一题" → 新题不与已有重复
6. 预览/打印/导出：分值来自结构定义，选项每项单独一行
