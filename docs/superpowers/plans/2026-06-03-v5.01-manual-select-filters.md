# V5.01 手工选题抽屉筛选条件增强 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 手工选题抽屉增加题型/难度/知识点三个筛选条件，统一知识点存储到 grade_level.knowledge_points（JSONB），清理 meta_data.knowledge_points 冗余

**Architecture:** 知识点筛选使用文本名称直传（非 UUID 桥接），前端 TreeSelect value=UUID label=名称，发送请求时提取 title 拼为逗号分隔字符串。后端 JSONB `@>` 精确匹配。grade_level.knowledge_points 成为唯一读写源，meta_data 列保留但不再使用 knowledge_points 键。

**Tech Stack:** FastAPI + SQLAlchemy + PostgreSQL 16 + React 19 + Ant Design 6.4 + Zustand 5 + TypeScript

**Spec:** `docs/superpowers/specs/2026-06-03-v5.01-manual-select-filters.md`

---

## 文件改动清单

| # | 文件 | 操作 | 条件 |
|---|------|------|------|
| 1 | `backend/app/schemas/question.py` | 删除顶层 knowledge_points，GradeLevel 加字段 | C1, C5 |
| 2 | `backend/app/api/v1/endpoints/questions.py` | create_question 死代码删、==→in_()、加 knowledge_points 过滤、update_question 修复 | C1, C2, C5 |
| 3 | `backend/app/api/v1/endpoints/question_admin.py` | LLM/爬取/去重改 grade_level，删 meta_data 写 | C6 |
| 4 | `backend/app/tasks/llm_tasks.py` | Celery 异步生成/爬取改 grade_level，删 meta_data 写 | C6 |
| 5 | `backend/seed_v35.py` | _q() 辅助函数加 grade_level.knowledge_points | C3 |
| 6 | `backend/seed_v4.py` | Question 构造加 grade_level.knowledge_points | C3 |
| 7 | `frontend/src/store/paperEditor.ts` | 加 knowledgeNodes 状态 | C4 |
| 8 | `frontend/src/pages/papers/steps/BasicInfoStep.tsx` | 加知识节点列表跨步骤共享 | C4 |
| 9 | `frontend/src/pages/papers/steps/RecommendStep.tsx` | 筛选面板、TreeSelect、debounce、清 console.log | 主功能 |
| 10 | `frontend/src/api/papers.ts` | getQuestions 参数扩展 | 主功能 |

---

### Task 1: 后端 Schema 清理 (C1 + C5)

**Files:**
- Modify: `backend/app/schemas/question.py`
- Modify: `backend/app/schemas/common.py`

- [ ] **Step 1: 从 GradeLevel 确认 knowledge_points 字段**

先检查 `backend/app/schemas/common.py` 中 `GradeLevel` 的定义:

```python
# 在 common.py 中找到 GradeLevel，确认是否已有 knowledge_points
# 如果没有，添加: knowledge_points: Optional[List[str]] = None
```

Read `backend/app/schemas/common.py` L25-45 确认 GradeLevel 结构。

- [ ] **Step 2: 从 QuestionBase 删除 knowledge_points**

```python
# backend/app/schemas/question.py L17
# 删除此行:
    knowledge_points: Optional[List[str]] = None
```

- [ ] **Step 3: 从 QuestionUpdate 删除 knowledge_points**

```python
# backend/app/schemas/question.py L45
# 删除此行:
    knowledge_points: Optional[List[str]] = None
```

- [ ] **Step 4: 确认 GradeLevel 有 knowledge_points**

如果 `common.py` 中 `GradeLevel` 尚未定义 `knowledge_points`，添加:

```python
# backend/app/schemas/common.py, GradeLevel 类内
knowledge_points: Optional[List[str]] = None
```

- [ ] **Step 5: 验证 Schema 变更**

```bash
cd backend && conda run -n ~/conda_workspace python -c "
from app.schemas.question import QuestionBase, QuestionCreate, QuestionUpdate
# 确认没有 knowledge_points 属性
print('QuestionBase fields:', [f for f in QuestionBase.model_fields.keys()])
print('QuestionCreate fields:', [f for f in QuestionCreate.model_fields.keys()])
print('QuestionUpdate fields:', [f for f in QuestionUpdate.model_fields.keys()])
# 确认 GradeLevel 有 knowledge_points
from app.schemas.common import GradeLevel
print('GradeLevel fields:', [f for f in GradeLevel.model_fields.keys()])
"
```

- [ ] **Step 6: Commit**

```bash
git add backend/app/schemas/question.py backend/app/schemas/common.py
git commit -m "refactor: C1/C5 删除 QuestionBase 顶层 knowledge_points，GradeLevel 补充字段

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 2: create_question 死代码清理 (C1)

**Files:**
- Modify: `backend/app/api/v1/endpoints/questions.py` L27-29

- [ ] **Step 1: 移除 create_question 中 meta_data 写入**

```python
# 改前 (L27-29):
data = question_in.model_dump(exclude={"content", "knowledge_points"}, exclude_none=True)
if question_in.knowledge_points:
    data["meta_data"] = {"knowledge_points": question_in.knowledge_points}
data["source"] = data.get("source") or "MANUAL"

# 改后:
data = question_in.model_dump(exclude={"content"}, exclude_none=True)
data["source"] = data.get("source") or "MANUAL"
```

注意：`exclude` 中移除 `knowledge_points`（该字段已从 QuestionCreate 删除，不再需要排除）。

- [ ] **Step 2: 验证**

```bash
cd backend && conda run -n ~/conda_workspace python -c "
from app.schemas.question import QuestionCreate
# 验证 QuestionCreate 无 knowledge_points
qc = QuestionCreate(title='test', question_type='SINGLE_CHOICE', difficulty='EASY',
    subject='math', score=5, correct_answer='A')
print('model_dump ok:', 'knowledge_points' not in qc.model_dump(exclude_none=True))
"
```

- [ ] **Step 3: Commit**

```bash
git add backend/app/api/v1/endpoints/questions.py
git commit -m "refactor: C1 移除 create_question 中死代码 meta_data.knowledge_points 写入

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 3: question_type / difficulty 改为 in_() (CRITICAL)

**Files:**
- Modify: `backend/app/api/v1/endpoints/questions.py` L638-641, L80-83

- [ ] **Step 1: 修复 get_questions (L638-641)**

```python
# 改前:
if question_type:
    query = query.where(Question.question_type == question_type)
if difficulty:
    query = query.where(Question.difficulty == difficulty)

# 改后:
if question_type:
    type_list = [t.strip() for t in question_type.split(",") if t.strip()]
    if type_list:
        query = query.where(Question.question_type.in_(type_list))
if difficulty:
    diff_list = [d.strip() for d in difficulty.split(",") if d.strip()]
    if diff_list:
        query = query.where(Question.difficulty.in_(diff_list))
```

- [ ] **Step 2: 追溯修复 search_questions (L80-83) — 同方法**

```python
# 改前 L80-83:
if question_type:
    query = query.where(Question.question_type == question_type)
if difficulty:
    query = query.where(Question.difficulty == difficulty)

# 改后:
if question_type:
    type_list = [t.strip() for t in question_type.split(",") if t.strip()]
    if type_list:
        query = query.where(Question.question_type.in_(type_list))
if difficulty:
    diff_list = [d.strip() for d in difficulty.split(",") if d.strip()]
    if diff_list:
        query = query.where(Question.difficulty.in_(diff_list))
```

- [ ] **Step 3: 验证后端可启动**

```bash
cd backend && conda run -n ~/conda_workspace python -c "from app.main import app; print('OK')"
```

- [ ] **Step 4: Commit**

```bash
git add backend/app/api/v1/endpoints/questions.py
git commit -m "fix: question_type/difficulty 从 == 改为 in_() 支持逗号分隔多选

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 4: GET /questions 新增 knowledge_points JSONB 过滤 (C2)

**Files:**
- Modify: `backend/app/api/v1/endpoints/questions.py` (get_questions 函数签名 + 过滤逻辑)

- [ ] **Step 1: 在函数签名添加参数**

在 `get_questions` 函数签名中，`difficulty` 后添加:

```python
knowledge_points: Optional[str] = None,  # 逗号分隔的知识点文本名称
```

- [ ] **Step 2: 添加过滤逻辑**

在 `difficulty` 过滤之后（L641 之后）、`review_status` 过滤之前:

```python
if knowledge_points:
    kp_list = [kp.strip() for kp in knowledge_points.split(",") if kp.strip()]
    if kp_list:
        query = query.where(
            Question.grade_level['knowledge_points'].contains(kp_list)
        )
```

- [ ] **Step 3: 验证**

```bash
cd backend && conda run -n ~/conda_workspace python -c "
from app.api.v1.endpoints.questions import get_questions
import inspect
sig = inspect.signature(get_questions)
print('knowledge_points in params:', 'knowledge_points' in sig.parameters)
"
```

- [ ] **Step 4: Commit**

```bash
git add backend/app/api/v1/endpoints/questions.py
git commit -m "feat: GET /questions 新增 knowledge_points 参数 JSONB @> 过滤

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 5: update_question 修复 (C5)

**Files:**
- Modify: `backend/app/api/v1/endpoints/questions.py` L552-555

- [ ] **Step 1: 移除 knowledge_points 的无效 setattr**

```python
# 改前 L552-555:
update_data = question_in.dict(exclude_unset=True)
for field, value in update_data.items():
    setattr(question, field, value)

# 改后:
update_data = question_in.dict(exclude_unset=True)
for field, value in update_data.items():
    setattr(question, field, value)
```

实际上不需要改动代码——C1 已经从 `QuestionUpdate` 删除了 `knowledge_points` 字段，所以 `update_data` 中不会再包含它。`grade_level`（包含 `knowledge_points` 子字段）通过 `setattr` 正常写入 JSONB 列。

验证: `QuestionUpdate` 已无 `knowledge_points`，`update_data` 中不会出现。

- [ ] **Step 2: Commit**

由于 C1 已覆盖，此 Task 无需额外改动。标记为已满足。

```bash
# 无需单独 commit — C1 的 schema 删除已修复此问题
```

---

### Task 6: question_admin.py 清理 (C6)

**Files:**
- Modify: `backend/app/api/v1/endpoints/question_admin.py` L158-179, L457-477, L522-523

- [ ] **Step 1: LLM 生成（同步）L179 — 移除 meta_data 写入**

```python
# 改前 L179:
meta_data={"knowledge_points": [knowledge_point]},

# 改后:
# 删除这行，只保留 grade_level JSONB 中的 knowledge_points（L163-164 已写入）
```

注意：`grade_level` JSONB 的 `knowledge_points` 已在 L163-164 设置，无需额外操作。只需删除 `meta_data={...}` 参数。

- [ ] **Step 2: 爬取（同步）L476 — 移除 meta_data 写入**

```python
# 改前 L476:
meta_data={"knowledge_points": kps},

# 改后:
# 删除这行，只保留 grade_level JSONB 中的 knowledge_points（L459-461 已写入）
```

- [ ] **Step 3: 去重过滤 L522-523 — 改为读 grade_level**

```python
# 改前:
if knowledge_point:
    query = query.where(Question.meta_data.contains({"knowledge_points": [knowledge_point]}))

# 改后:
if knowledge_point:
    query = query.where(
        Question.grade_level['knowledge_points'].contains([knowledge_point])
    )
```

- [ ] **Step 4: 验证**

```bash
cd backend && conda run -n ~/conda_workspace python -c "from app.main import app; print('OK')"
```

- [ ] **Step 5: Commit**

```bash
git add backend/app/api/v1/endpoints/question_admin.py
git commit -m "refactor: C6 question_admin LLM/爬取/去重改为仅用 grade_level.knowledge_points

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 7: llm_tasks.py 清理 (C6)

**Files:**
- Modify: `backend/app/tasks/llm_tasks.py` L92, L175

- [ ] **Step 1: Celery LLM 生成 L92 — 移除 meta_data 写入**

```python
# 改前 L92:
meta_data={"knowledge_points": [knowledge_point]},

# 改后:
# 删除这行
```

- [ ] **Step 2: Celery 爬取 L175 — 移除 meta_data 写入**

```python
# 改前 L175:
meta_data={"knowledge_points": kps},

# 改后:
# 删除这行
```

- [ ] **Step 3: 验证**

```bash
cd backend && conda run -n ~/conda_workspace python -c "from app.tasks.llm_tasks import process_llm_generation; print('OK')"
```

- [ ] **Step 4: Commit**

```bash
git add backend/app/tasks/llm_tasks.py
git commit -m "refactor: C6 Celery 异步任务改为仅用 grade_level.knowledge_points

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 8: /search 端点和 export 端点 meta_data → grade_level (C6)

**Files:**
- Modify: `backend/app/api/v1/endpoints/questions.py` L186-197, L80-83

- [ ] **Step 1: export_questions 的 knowledge_point 过滤（L186-197）**

当前实现是用 Python 内存过滤 `meta_data.get("knowledge_points", [])`。改为读 `grade_level`:

```python
# 改前 L195-196:
meta = q.meta_data or {}
kps = meta.get("knowledge_points", [])

# 改后:
gl = q.grade_level or {}
kps = gl.get("knowledge_points", [])
```

- [ ] **Step 2: 检查 search_questions 的 knowledge_point 搜索（L91）**

`search_questions` 中的 keyword 搜索使用 `grade_level['knowledge_points'].cast(String).ilike(...)` — 这已经是用 grade_level 而非 meta_data，不需要改。但检查是否有其他引用:

```bash
grep -n "meta_data.*knowledge_points" backend/app/api/v1/endpoints/questions.py
```

应返回空结果或仅剩注释。

- [ ] **Step 3: 验证**

```bash
cd backend && conda run -n ~/conda_workspace python -c "from app.main import app; print('OK')"
```

- [ ] **Step 4: Commit**

```bash
git add backend/app/api/v1/endpoints/questions.py
git commit -m "refactor: C6 /search 和 export 端点 knowledge_points 改用 grade_level

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 9: 种子脚本修复 (C3)

**Files:**
- Modify: `backend/seed_v35.py`
- Modify: `backend/seed_v4.py`

- [ ] **Step 1: seed_v35.py — _q() 辅助函数添加 knowledge_points**

当前 `_q()` (L163-177) 将 `grade_level` 构建为 `{scope, grades}`，不含 `knowledge_points`。需要在适当位置添加知识点数据。

```python
# seed_v35.py _q() 辅助函数，修改 grade_level 构造
# 改前 L166:
grade_level = json.dumps({"scope": grade_scope, "grades": grades})

# 改后: 添加 knowledge_points（如果有的话）
# 注意: _q() 当前不接受 knowledge_points 参数
# 方案: 在 _q() 添加可选参数 knowledge_points=None
# 如果提供，写入 grade_level.knowledge_points

# 修改 _q() 签名:
def _q(title, question_type, difficulty, subject, grade_scope, grades,
       score, correct_answer, explanation="", options=None, knowledge_points=None):
    grade_level_data = {"scope": grade_scope, "grades": grades}
    if knowledge_points:
        grade_level_data["knowledge_points"] = knowledge_points
    grade_level = json.dumps(grade_level_data)
    return {
        ...
        "grade_level": grade_level,
        ...
    }
```

同时为种子题目添加 `knowledge_points` 参数——选择具有代表性的知识点标签。

- [ ] **Step 2: seed_v4.py — Question 构造添加 grade_level**

当前 seed_v4.py 的 Question 构造（L131-142）不设置 `grade_level`（None）。如果这些题目不需要知识点筛选可保持 None——JSONB `@>` 在 NULL 上返回 false，不影响筛选。

但为了一致性，最好至少设置 `grade_level={"scope": "grade", "grades": [...], "knowledge_points": [...]}`。

- [ ] **Step 3: 验证种子脚本**

```bash
cd backend && conda run -n ~/conda_workspace python -c "
# 语法检查
import py_compile
py_compile.compile('seed_v35.py', doraise=True)
py_compile.compile('seed_v4.py', doraise=True)
print('Syntax OK')
"
```

- [ ] **Step 4: Commit**

```bash
git add backend/seed_v35.py backend/seed_v4.py
git commit -m "fix: C3 种子脚本确保 grade_level.knowledge_points 有数据

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 10: paperEditor store 加 knowledgeNodes (C4)

**Files:**
- Modify: `frontend/src/store/paperEditor.ts`
- Modify: `frontend/src/types/paper.ts`

- [ ] **Step 1: PaperDraft 或 store 加 knowledgeNodes**

在 `PaperEditorState` 接口中添加（不在 PaperDraft 上，因为这是 UI 缓存数据）:

```typescript
// frontend/src/store/paperEditor.ts
interface PaperEditorState {
  // ... existing fields
  knowledgeNodes: any[];  // 知识节点列表，BasicInfoStep 加载后跨步骤共享
}

// 初始值:
const initialState = {
  // ...
  knowledgeNodes: [],
};

// 添加 setter:
setKnowledgeNodes: (nodes: any[]) => set({ knowledgeNodes: nodes }),
```

- [ ] **Step 2: 验证 TypeScript**

```bash
cd frontend && npx tsc --noEmit --pretty false 2>&1 | head -20
```

- [ ] **Step 3: Commit**

```bash
git add frontend/src/store/paperEditor.ts
git commit -m "feat: C4 paperEditor store 添加 knowledgeNodes 跨步骤共享

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 11: BasicInfoStep 知识节点存入 store (C4)

**Files:**
- Modify: `frontend/src/pages/papers/steps/BasicInfoStep.tsx`

- [ ] **Step 1: 加载知识树后存入 store**

```typescript
// 在 loadKnowledgeTree 函数末尾添加:
const loadKnowledgeTree = async (sid: string) => {
  // ... 现有逻辑 ...
  setKnowledgeNodes(points);
  setKnowledgeNodesStore(points);  // 新增: 存入 store 供其他 Step 使用
};
```

需要从 store 获取 `setKnowledgeNodes`:
```typescript
const setKnowledgeNodesStore = usePaperEditorStore(s => s.setKnowledgeNodes);
```

- [ ] **Step 2: 验证 TypeScript**

```bash
cd frontend && npx tsc --noEmit --pretty false 2>&1 | head -20
```

- [ ] **Step 3: Commit**

```bash
git add frontend/src/pages/papers/steps/BasicInfoStep.tsx
git commit -m "feat: C4 BasicInfoStep 知识节点列表存入 store 跨步骤共享

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 12: RecommendStep 筛选面板 + 清理 (主功能)

**Files:**
- Modify: `frontend/src/pages/papers/steps/RecommendStep.tsx`

这是工作量最大的 Task。需要添加:

1. 筛选状态变量
2. 筛选面板 UI（折叠）
3. TreeSelect 知识点选择器（从 store 读取 knowledgeNodes）
4. debounce 搜索
5. 自动填充逻辑
6. 清理 console.log
7. 防御性 unit 检查

- [ ] **Step 1: 添加筛选状态和 import**

在 L69-74 状态区添加:

```typescript
// 筛选条件状态
const [filterExpanded, setFilterExpanded] = useState(true);  // 默认展开
const [filterTypes, setFilterTypes] = useState<string[]>([]);
const [filterDifficulties, setFilterDifficulties] = useState<string[]>(['EASY', 'MEDIUM', 'HARD']);
const [filterKnIds, setFilterKnIds] = useState<string[]>([]);
```

添加新 import:
```typescript
import { TreeSelect, Button, Space } from 'antd';
import { FilterOutlined } from '@ant-design/icons';
```

从 store 读取 knowledgeNodes:
```typescript
const knowledgeNodes = usePaperEditorStore(s => s.knowledgeNodes);
```

- [ ] **Step 2: 修改 openManualSelect 添加自动填充**

```typescript
const openManualSelect = (questionId: string, unitId: string, questionType: string) => {
  setManualTarget({ questionId, unitId, questionType });
  setManualKeyword('');
  setManualResults([]);
  setManualOpen(true);

  // 自动填充: 题型从 unit.question_config 读取
  const livePaper = getLivePaper();
  const unit = livePaper?.units?.find(u => u.id === unitId);
  if (unit?.question_config?.length) {
    const types = [...new Set(unit.question_config.map((c: any) => c.question_type))];
    setFilterTypes(types);
  } else {
    setFilterTypes([]);
  }

  // 自动填充: 难度从 paper.difficulty_ratio 读取
  const ratio = livePaper?.difficulty_ratio;
  if (ratio) {
    const diffs = Object.entries(ratio)
      .filter(([_, v]) => (v as number) > 0)
      .map(([k]) => k);
    setFilterDifficulties(diffs.length > 0 ? diffs : ['EASY', 'MEDIUM', 'HARD']);
  } else {
    setFilterDifficulties(['EASY', 'MEDIUM', 'HARD']);
  }

  // 自动填充: 知识点从 paper.knowledge_node_ids 读取 (TreeSelect value = UUID)
  setFilterKnIds(livePaper?.knowledge_node_ids || []);

  // 立即搜索
  fetchManualResults(questionType, '', types,
    ratio ? Object.entries(ratio).filter(([_, v]) => (v as number) > 0).map(([k]) => k) : ['EASY', 'MEDIUM', 'HARD'],
    livePaper?.knowledge_node_ids || []
  );
};
```

- [ ] **Step 3: 修改 fetchManualResults 支持筛选参数**

```typescript
const fetchManualResults = async (
  questionType: string,
  keyword: string,
  types?: string[],
  diffs?: string[],
  knIds?: string[]
) => {
  setManualLoading(true);
  try {
    const livePaper = getLivePaper();
    const rawGrades = livePaper?.grade_level?.grades;
    const grades: string[] = Array.isArray(rawGrades)
      ? rawGrades
      : typeof rawGrades === 'string'
        ? [rawGrades]
        : [];

    // 从 knowledgeNodes 中提取文本名称（UUID → title）
    const selectedTitles = (knIds || filterKnIds)
      .map(id => knowledgeNodes.find((n: any) => n.key === id)?.title)
      .filter(Boolean) as string[];

    const params: any = {
      question_type: (types || filterTypes).join(",") || undefined,
      difficulty: (diffs || filterDifficulties).join(",") || undefined,
      knowledge_points: selectedTitles.join(",") || undefined,
      subject: livePaper?.subject || undefined,
      keyword: keyword || undefined,
      review_status: 'APPROVED',
      limit: 50,
    };
    if (grades.length === 1) params.grade = grades[0];
    else if (grades.length > 1) params.grade_level = grades[0];

    const resp = await paperApi.getQuestions(params);
    const data = Array.isArray(resp.data) ? resp.data : (resp.data?.items || resp.data?.data || []);

    const allQids = new Set(
      (getLivePaper()?.units || []).flatMap(u => (u.questions || []).map(q => q.question_id))
    );
    const filtered = data.filter((q: any) => !allQids.has(q.id));
    setManualResults(filtered);
  } catch (e: any) {
    message.error(e?.response?.data?.message || e?.message || '搜索失败');
    setManualResults([]);
  } finally {
    setManualLoading(false);
  }
};
```

- [ ] **Step 4: 添加 debounce hook**

```typescript
import { useMemo, useRef, useCallback } from 'react';

// 在组件内:
const debounceTimer = useRef<ReturnType<typeof setTimeout>>();

const debouncedSearch = useCallback((questionType: string, keyword: string) => {
  if (debounceTimer.current) clearTimeout(debounceTimer.current);
  debounceTimer.current = setTimeout(() => {
    fetchManualResults(questionType, keyword);
  }, 300);
}, []);
```

- [ ] **Step 5: 修改 Drawer 渲染，添加筛选面板**

在 Drawer 内，Input.Search 下方添加:

```tsx
<Drawer
  title="手工选题"
  open={manualOpen}
  onClose={() => setManualOpen(false)}
  width={600}
>
  <div style={{ marginBottom: 16 }}>
    <Input.Search
      placeholder="搜索题目关键词..."
      value={manualKeyword}
      onChange={(e) => {
        setManualKeyword(e.target.value);
        debouncedSearch(manualTarget?.questionType || '', e.target.value);
      }}
      onSearch={(v) => fetchManualResults(manualTarget?.questionType || '', v)}
      enterButton={<><SearchOutlined /> 搜索</>}
      allowClear
    />
  </div>

  {/* 筛选面板 */}
  <div style={{ marginBottom: 12 }}>
    <Button
      size="small"
      icon={<FilterOutlined />}
      onClick={() => setFilterExpanded(!filterExpanded)}
      type={filterExpanded ? 'primary' : 'default'}
    >
      筛选 {filterExpanded ? '▲' : '▼'}
    </Button>
    {filterExpanded && (
      <div style={{
        background: '#fafafa', borderRadius: 6, padding: 10, marginTop: 8,
        display: 'flex', gap: 8, flexWrap: 'wrap'
      }}>
        <Select
          mode="multiple"
          placeholder="题型"
          value={filterTypes}
          onChange={(v) => { setFilterTypes(v); fetchManualResults(manualTarget?.questionType || '', manualKeyword); }}
          options={[
            { value: 'SINGLE_CHOICE', label: '单选题' },
            { value: 'MULTIPLE_CHOICE', label: '多选题' },
            { value: 'FILL_BLANK', label: '填空题' },
            { value: 'SUBJECTIVE', label: '解答题' },
          ]}
          style={{ minWidth: 120, flex: 1 }}
          size="small"
          allowClear
        />
        <Select
          mode="multiple"
          placeholder="难度"
          value={filterDifficulties}
          onChange={(v) => { setFilterDifficulties(v); fetchManualResults(manualTarget?.questionType || '', manualKeyword); }}
          options={[
            { value: 'EASY', label: '简单' },
            { value: 'MEDIUM', label: '中等' },
            { value: 'HARD', label: '困难' },
          ]}
          style={{ minWidth: 120, flex: 1 }}
          size="small"
          allowClear
        />
        <TreeSelect
          treeData={knowledgeNodes.map((n: any) => ({
            value: n.key,
            title: n.title,
            label: n.title,
            children: n.children,
            isLeaf: n.node_type === 'POINT',
          }))}
          placeholder="知识点"
          treeCheckable
          showCheckedStrategy={TreeSelect.SHOW_CHILD}
          value={filterKnIds}
          onChange={(v) => { setFilterKnIds(v); fetchManualResults(manualTarget?.questionType || '', manualKeyword); }}
          style={{ minWidth: 160, flex: 1 }}
          size="small"
          allowClear
          maxTagCount={2}
        />
      </div>
    )}
  </div>

  <Spin spinning={manualLoading}>
    {/* ... 结果卡片 ... */}
  </Spin>
</Drawer>
```

- [ ] **Step 6: 增强结果卡片显示分数和知识点**

```tsx
{manualResults.map((q: any) => (
  <Card
    key={q.id}
    size="small"
    hoverable
    style={{ marginBottom: 8 }}
    onClick={() => handleManualReplace(q)}
  >
    <div style={{ fontSize: 13, lineHeight: 1.5 }}>{q.title?.substring(0, 120)}</div>
    <div style={{ marginTop: 4, display: 'flex', gap: 4, flexWrap: 'wrap', alignItems: 'center' }}>
      <Tag color={DIFF_COLORS[q.difficulty] || 'default'} style={{ fontSize: 10 }}>
        {DIFF_LABELS[q.difficulty] || q.difficulty}
      </Tag>
      <Tag color="blue" style={{ fontSize: 10 }}>
        {QTYPE_LABELS[q.question_type] || q.question_type}
      </Tag>
      {q.score != null && <Tag color="orange" style={{ fontSize: 10 }}>{q.score}分</Tag>}
      {(q.grade_level?.knowledge_points || []).slice(0, 2).map((kp: string, i: number) => (
        <Tag key={i} color="purple" style={{ fontSize: 10 }}>{kp}</Tag>
      ))}
      <span style={{ fontSize: 11, color: '#999' }}>点击替换</span>
    </div>
  </Card>
))}
```

- [ ] **Step 7: 防御性 unit 检查**

在 `handleManualReplace` 开头添加:

```typescript
const handleManualReplace = (newQ: any) => {
  if (!manualTarget || !paper) return;
  const { questionId, unitId } = manualTarget;
  const unit = paper.units?.find(u => u.id === unitId);
  if (!unit) {
    message.error('该单元已被移除，请关闭后重新打开');
    setManualOpen(false);
    return;
  }
  // ... 继续现有逻辑 ...
};
```

- [ ] **Step 8: 清理 console.log**

删除 L91, 92, 111, 114, 115, 118, 125, 128 处所有 `console.log('[手工选题] ...')` 语句。

- [ ] **Step 9: 验证 TypeScript + Vite build**

```bash
cd frontend && npx tsc --noEmit --pretty false 2>&1 | head -30
cd frontend && npm run build 2>&1 | tail -10
```

- [ ] **Step 10: Commit**

```bash
git add frontend/src/pages/papers/steps/RecommendStep.tsx
git commit -m "feat: V5.01 手工选题抽屉增加题型/难度/知识点筛选条件

- 折叠筛选面板（默认展开）
- TreeSelect 知识点选择器（从 store 读取 knowledgeNodes）
- 自动填充题型/难度/知识点
- debounce 300ms 自动搜索
- 结果卡片增强（分数、知识点标签）
- 防御性 unit 检查
- 清理 8 处 console.log 调试语句

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 13: API 客户端参数扩展

**Files:**
- Modify: `frontend/src/api/papers.ts`

- [ ] **Step 1: 验证 getQuestions 已支持扩展参数**

`papers.ts` L52:
```typescript
getQuestions: (params?: any) => apiClient.get('/questions', { params }),
```

此方法已经接受 `any` 类型 params，无需修改。新增的 `knowledge_points` 参数会自动传递。

- [ ] **Step 2: 无需改动，跳过 Commit**

---

### Task 14: 端到端验证

- [ ] **Step 1: 后端测试**

```bash
cd backend && conda run -n ~/conda_workspace pytest -v 2>&1 | tail -20
```

- [ ] **Step 2: 确认 TypeScript 0 错误**

```bash
cd frontend && npx tsc --noEmit --pretty false 2>&1 | wc -l
# 预期: 0
```

- [ ] **Step 3: 确认 Vite build 通过**

```bash
cd frontend && npm run build 2>&1 | tail -5
# 预期: built in ...s
```

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "chore: V5.01 端到端验证通过 — 测试+TS+build 全绿

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## 执行顺序

```
Task 1 (Schema) → Task 2 (create_question) → Task 3 (==→in_)
  → Task 4 (knowledge_points 过滤) → Task 5 (已满足)
  → Task 6 (question_admin) → Task 7 (llm_tasks) → Task 8 (/search)
  → Task 9 (种子脚本) → Task 10 (store) → Task 11 (BasicInfoStep)
  → Task 12 (RecommendStep 主功能) → Task 13 (已满足) → Task 14 (验证)
```

Tasks 1-9 是后端（可批量执行），Tasks 10-12 是前端（有依赖），Task 14 最终验证。
